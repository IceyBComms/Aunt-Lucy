<!-- CANONICAL COPY. Moved into the repo on 23 August 2026 so BOTH Claude Code and Cowork can read and append to it. -->

# BUGS_AND_FIXES.md — Known Issues and Their Status

> 🗂️ **THIS IS THE CANONICAL BUG RECORD, as of 23 August 2026.** **It lives in the REPO so Claude Code can read its own history instead of asking; Cowork writes to the same file.** ⚠️ **A stale copy exists at `C:\Users\Kate\OneDrive\aunt-lucy\build\BUGS_AND_FIXES.md` — it is marked superseded and should not be edited. This one wins.**
>
> **Why it moved:** the code lives in `Documents\Aunt-Lucy` and the notes live in `OneDrive\aunt-lucy`. Claude Code could only ever see the first, so it had no access to the history of the bugs it was fixing — it needed #039, #045 and #058 in one day and could read none of them. Kate was the courier. Now she isn't.
>
> **How to use it:** append new bugs at the TOP of the open table, keep the numbering sequential, and never delete a row — annotate it instead. Status: 🔴 open · 🟡 fixed, merged and deployed but NOT yet seen working · 🟢 someone has looked at the real thing and it was right.

---

> Log bugs here as you find them. Don't try to fix everything at once.

---

## Status key
- 🔴 **Open** — not yet fixed
- 🟡 **In progress** — being worked on
- 🟢 **Fixed** — resolved (keep for reference)

---

## Open bugs

| # | Status | Description | Found | Priority |
|---|--------|-------------|-------|----------|
| 066 | 🔴 | **THE BUYER NEVER SEES THE OCCASION ECHOED BACK BEFORE PAYING — add one line to the buyer receipt: *"Sent as: Loss"*.** 🔎 **Traced end to end 23 Aug: submit goes STRAIGHT to Stripe (`window.location.assign(checkoutUrl)`, no review step); the Stripe line item is per-TIER ("One employee — $79"), not per-occasion; and the buyer confirmation carries `tierName(amountCents)` only (`giftFulfilment.ts:166`) — `occasion` is not among its params.** ❗ **So the FIRST time the occasion is visible to any human is the recipient's delivery email, which is the one place it is too late.** ⚖️ **This is the second net under #062: #062 stops the silent default, this catches a genuine mis-tap — after the money moves, but before the recipient opens anything.** 📌 **Cheap: one line, one file, no schema.** | 23 Aug 2026 (Claude Code, while fixing #062) | 🟡 Medium | Logged not built — Kate's call, 23 Aug. |
| 065 | 🔴 | **THE TEAM-SIGNING PLACEHOLDER IS FIXED-TONE FOR ALL FIVE OCCASIONS — AND THE SIGNING PAGE NEVER RECEIVES THE OCCASION AT ALL.** `GiftSigning.tsx:286` hard-codes *"Thinking of you — the whole team's got your back. Take all the time you need."* ⚠️ **DIRECTION IS THE OPPOSITE OF EXPECTED: it is condolence-flavoured, so it is wrong on a NEW BABY card, not on a bereavement one.** ❗ **This matters MORE than #064: a dozen colleagues are each prompted by that example and each writes toward it, so one wrong prompt yields a dozen wrong messages — and the recipient reads all of them in one sitting.** 🔒 **BLOCKER, not just a string swap: `SignCardContext` carries only `recipientFirstName`, `organiserName`, `organisationName`, `signedCount`, `closed` — NO occasion. Fixing this needs `occasion` added to the schema in `lib/api-spec/openapi.yaml`, the `/sign/:token` handler, and an Orval regen. #064 is a string change; this is an API change.** | 23 Aug 2026 (Claude Code sweep of the class, at Kate's instruction) | 🔴 High | Not built — reported for Kate's ruling first. | ✅ **23 Aug — SCOPED, and smaller than it looks: `gift.occasion` is ALREADY on the row the handler has loaded, so NO migration and no DB change. Four steps: add `occasion` (nullable) to `SignCardContext` in `openapi.yaml` (~6 lines) · Orval regen (generated, never hand-edited) · return it from `giftCards.ts:66` (ONE line, `gift` already in scope) · placeholder from a `Record<Occasion, string>` in `GiftSigning.tsx:286` (~10 lines). ~30 minutes, an hour with rehearsal.** ⚠️ **The real cost isn't lines — it touches the API contract and regenerates two packages, so the DIFF looks far bigger than the change and needs a careful read. That is exactly why it gets its own session.** ⚖️ **DESIGN CALL SETTLED: the placeholder map stays CLIENT-SIDE beside `inviteCopyHints.ts`. It's display-only ghost text, never sent, never stored — server-side would imply it's part of the message contract, which it isn't.** 🎯 **23 Aug — THE CARE-NOT-COMMITMENT RULE APPLIES HERE HARDEST (see #064). THREE of the five signing drafts broke it and are gone: *"the whole team's got your back"* · *"we're here whenever you need us"* · *"the whole team's here"*. All warm, all readable as an offer of practical help, all replaced with care-only wording. A dozen colleagues are each nudged by that placeholder — it is the single highest-leverage string in the employer path.** 📐 **SCOPED 23 Aug (Claude Code). NO MIGRATION, NO DB CHANGE — `gift.occasion` is already on the row the handler loads.** (1) add `occasion` (nullable `GiftOccasion`) to `SignCardContext` in `lib/api-spec/openapi.yaml`, ~6 lines · (2) Orval regen into `lib/api-client-react` + `lib/api-zod`, generated, never hand-edited · (3) return it — **ONE line** at `giftCards.ts:66`, `gift` is already in scope · (4) placeholder from a `Record<Occasion, string>` at `GiftSigning.tsx:286`, ~10 lines. **~30 min of work, ~1 hour with rehearsal; the real cost is that it touches the API contract and regenerates two packages, so the diff reads bigger than the change.** ✅ **KATE'S RULING 23 Aug: the placeholder map lives CLIENT-SIDE next to `inviteCopyHints.ts` — display-only ghost text, never sent, never stored; server-side would imply it is part of the message contract, which it is not.** ✅ **APPROVED IN PRINCIPLE, own session, not bundled. The five strings:** new baby *"e.g. So happy for you — enjoy every second."* · illness *"e.g. Thinking of you — take all the time you need."* · loss *"e.g. Thinking of you — the whole team's got your back."* · ongoing *"e.g. Thinking of you — we're here whenever you need us."* · other *"e.g. Thinking of you — the whole team's here."* |
| 064 | 🔴 | **The buy form's note placeholder is upbeat on EVERY occasion — *"e.g. From all of us at Brightpath — enjoy every minute"* shows on Loss.** ✅ **Kate's catch, 23 Aug, from the #062 preview. Same class as everything else today: one string written for one occasion and shown to all five.** 📌 **Cheap NOW because of #062 — the occasion is chosen before the buyer reaches that field, so the example can follow it.** ⚖️ **Worth doing WELL rather than merely neutrally: a buyer sending a bereavement gift genuinely doesn't know what to write, so the example is the most useful thing on that screen. Drafts written for all five.** ❓ **Open question first: does this same form serve CONSUMER buyers as well as workplace? *"From all of us at Brightpath"* is employer-flavoured — if an individual buying for a friend sees it, that's a second variant, not a translation.** | 23 Aug 2026 (buy form preview) | 🟡 Medium | Drafts sent. | ❗❗ **KATE EXTENDED IT, AND THE EXTENSION MATTERS MORE THAN THE ORIGINAL: the same fault will be in the TEAM SIGNING flow, where colleagues add their messages to the card. That one is worse — a dozen colleagues are EACH prompted by that example, and each writes toward it. An upbeat prompt on a bereavement card produces a dozen upbeat messages, and the recipient reads ALL of them.** ⚖️ **So this stops being one string and becomes a CLASS: every placeholder, example, prompt and suggested-text string across the purchase and team-signing flows, with the occasions each appears under. Full list requested BEFORE any fixing — see the class, don't patch instances.** 🔎 **GATE QUESTION ANSWERED (Claude Code, 23 Aug): YES — the SAME form serves consumer buyers. `/buy/:tierId` is one component; `isWorkplace` is true ONLY for `workplace_individual` (`BuyDetails.tsx:40`), so `consumer_personal` renders the same fields.** ❗ **The placeholder ALREADY has two tier variants — workplace *"e.g. From all of us at Brightpath — enjoy every minute."* (`:376`) and consumer *"e.g. Thinking of you. Lean on us — that's what we're here for."* (`:377`). So this is 2 variants × 5 occasions = **10 strings**, not 5, and Kate's five drafts are all workplace-flavoured ("From all of us at Brightpath") — the consumer set needs writing separately, in a friend's voice not an employer's.** ⚠️ **The consumer variant is ALREADY mis-toned in the other direction: *"Thinking of you. Lean on us"* reads as condolence and is shown on a NEW BABY gift.** 📌 **Not built — reported for Kate's ruling on the consumer wording first. See #065 for the same fault in team signing, which is worse and needs an API change.** | 🔄 **23 Aug — THE DIRECTION IS THE OPPOSITE OF WHAT BOTH KATE AND COWORK PREDICTED, and it's worth keeping. We reasoned: an UPBEAT prompt on a bereavement card produces a dozen upbeat messages. The mechanism was right; the direction was backwards. The signing placeholder is CONDOLENCE-flavoured — *"Thinking of you — the whole team's got your back. Take all the time you need."* — and it lands on a NEW BABY card. A dozen colleagues each nudged toward solemnity at a baby shower.** 📌 **Same on the buy form: the CONSUMER placeholder reads *"Thinking of you. Lean on us"*, which is what a baby-shower buyer sees today.** ⚖️ **AND IT'S TEN STRINGS, NOT FIVE: the placeholder already has two TIER variants (workplace `:376`, consumer `:377`), neither occasion-aware — so 2 tiers × 5 occasions. Both sets now drafted; the consumer voice is a friend talking, not a company.** 🔑 **Reasoning correctly about HOW something fails does not tell you WHICH WAY it fails. Only looking does.** ✍️ **23 Aug — TEN STRINGS DRAFTED AND NINE APPROVED FIRST TIME. The one genuine clunk was caught by Code: *"Can't wait to meet them. Let us do the dinners"* switches voice mid-string — *"can't wait to meet them"* is one friend, *"let us"* is a committee. Replaced with *"Can't wait to meet them. Let me take the first dinner."* — one speaker who then gestures at the group, matching the pattern of the loss line, AND modelling the exact behaviour the product wants: claim ONE thing.** 🎯🎯 **23 Aug — KATE'S RULE, and it decides every workplace string in the product: CONSUMER copy may offer HELP; WORKPLACE and SIGNING copy may only offer CARE.** ⚖️ **Because the two buyers stand in completely different relationships to the page. A FRIEND who buys is probably one of the helpers, so *"let me take the first dinner"* is perfect there — it models exactly what the product wants, one person claiming one thing. COLLEAGUES ARE NOT THE HELPER POOL: they chipped in for a gift, and the people who help are the friends and family she chooses.** ❗ **So any workplace or signing string implying the writer will personally turn up — *"we've got things covered"*, *"we've got your back"*, *"here whenever you need us"* — feeds #061 directly: she reads a card full of colleagues apparently volunteering, concludes it's handled, and invites NOBODY.** ⚠️ **Cowork's OWN approved workplace string broke the rule: *"look after yourself, we've got things covered here"* was praised for meaning WORK is covered, but a recipient can just as easily read it as the MEALS are covered — ambiguous in exactly the wrong direction. Revised to *"Work's covered, don't give it a thought."*** ✅ **COPY FINAL, KATE 23 Aug — ALL TEN STRINGS APPROVED. Workplace:** new baby *"e.g. From all of us at Brightpath — enjoy every minute."* · illness *"…look after yourself, we've got things covered here."* · loss *"…we're so sorry. Take whatever time you need."* · ongoing *"…thinking of you, and here whenever you need us."* · other *"…thinking of you."* **Consumer:** new baby *"e.g. Can't wait to meet them. Let me take the first dinner."* · illness *"e.g. Thinking of you. Let us take a few things off your plate."* · loss *"e.g. I'm so sorry. I'm here, and so is everyone else."* · ongoing *"e.g. Thinking of you — here for the long haul, not just this week."* · other *"e.g. Thinking of you. Lean on us — that's what we're here for."* 🔧 **The consumer new-baby line was revised from *"Let us do the dinners"*: the original switched voice mid-string (singular *"Can't wait to meet them"*, then a committee *"let us"*), where the loss line already solved that — one speaker who then gestures at the group. *"Let me take the first dinner"* also models the behaviour the product wants: claim ONE thing.** 📌 **Still not built — copy is settled, the change is not.** |
| 063 | 🔴 | **THERE IS NO CORRECTION PATH FOR A WRONGLY-STAMPED OCCASION. Not for Kate, not the buyer, not the recipient.** 🔎 **Code grepped every `.set(` and `update(` touching `occasion` across the API — ZERO hits. It's written once at gift creation (`gifts.ts:246`), copied to the page at activation, and never writable again.** ⚖️ **Note the asymmetry: the recipient CAN change her pronouns, the baby stage, both invite lines and every single task — but NOT the one field that determines the most words on her page.** ❗ **Today the only recovery for a mis-stamped gift is a direct database edit. Worth knowing BEFORE it's needed rather than after.** 📌 **Less likely once #062 lands, but a buyer can still err, and a situation can change.** | 23 Aug 2026 (from the #062 investigation) | 🟡 Medium | Logged, not built. | 📌 **23 Aug — related but distinct: #066 covers the missing buyer-receipt echo. This row stays the missing CORRECTION path.**
| 062 | 🟢 | 🚨 **THE OCCASION PICKER SHIPS PRE-SELECTED ON `new_baby`, AND NOTHING FORCES A DELIBERATE CHOICE — so a buyer who doesn't notice that row of buttons sends a bereaved person *"A new little person is part of your world now."*** `BuyDetails.tsx:54` initialises `useState<GiftOccasion>("new_baby")` and `occasion` posts straight through at `:128`. **The recipient then also gets the new-baby task list and *"Has the baby arrived yet?"* on her activation page.** ❗ **LIVE on `main` (`2e6b5da`, confirmed against `/api/healthz`); `/buy` returns 200.** 🔑 **Found while DISPROVING the #060 false alarm — a different route to precisely the outcome that scare was about, and this one is the product rather than a mock.** ⚖️ **RULED: require a deliberate selection. Code was right to flag it as a product call since it adds checkout friction — but the asymmetry settles it. The cost is ONE TAP on the single field that determines every word the recipient reads; the cost of the error is a bereaved person opening new-baby copy, which is not a bug you apologise for. And there IS no correct default here: defaulting saves a tap for some buyers and destroys the product for others.** | 23 Aug 2026 (from the #060 investigation) | 🔴🔴 **Critical — live** | Approved, own branch, nothing bundled. | ✅ **BUILT 23 Aug on `claude/bug-060-occasion-required` — `useState<GiftOccasion | null>(null)`, submit disabled until a choice, PLUS a second guard in the handler so a stray submit can't post without one.** 🔎 **And Code checked the *looks*-pre-chosen risk properly: its first probe gave a FALSE POSITIVE by matching `hover:border-primary/30` in the class names, so it re-checked COMPUTED styles off the live DOM. All five pills are byte-identical until one is picked — pale border `rgb(235,231,224)`, muted text — while "Email" and "Now" elsewhere on the same page ARE selected with dark green outlines, so the occasion row reads unmistakably as "not yet".** ✍️ **New microcopy approved: *"Choose the occasion above to continue."* — a disabled button with no explanation is its own trap.** ⚠️ **AND A SECOND GAP CONFIRMED: the occasion is NEVER echoed back before payment — no review step, the Stripe line item is per TIER ("One employee — $79"), and the buyer receipt passes only `tierName`. The first time any human sees the occasion is the recipient's delivery email, which is the one place it's too late. Suggested follow-up: one line on the buyer receipt, *"Sent as: Loss"*.** ✅ **FIXED + MERGED + VERIFIED ON PROD, 23 Aug (PR #72).** Occasion starts `null`, submit disabled until chosen, second guard in the submit handler so a stray submit cannot post without one. 🔎 **Computed styles read off the LIVE production DOM, not inferred from class names: unchosen = border `rgb(235,231,224)` / white / text `rgb(142,126,113)`; chosen = border `rgb(46,107,80)` / green tint / text `rgb(43,43,43)`. All five identical until one is picked, so nothing reads as pre-selected.** 🟢 **Green: probed `https://www.auntlucy.com.au/buy/workplace_individual` after deploy — all five `aria-pressed=false`, submit `disabled=true`, hint present.** 📌 **Kate approved the microcopy *"Choose the occasion above to continue."* — a disabled button with no explanation is its own trap.** |
| 061 | 🔴 | **THE EMPLOYER SIGNING BLOCK IMPLIES THE COLLEAGUES ARE THE HELPERS.** The page reads *"GIFTED BY THE TEAM AT MERIDIAN"* · *"SIGNED BY YOUR TEAM"* · *"The team at Meridian"* — and then asks the recipient to add her people. ⚠️ **Kate, 23 Aug: a recipient can reasonably conclude her COLLEAGUES have signed up to help.** ❗ **Two failure paths, both ending in an empty page: she invites NOBODY because she thinks it's handled · or she expects colleagues to arrive with meals and they never do.** ⚖️ **The employer gift means the employer PAYS; the helpers are her own friends and family. Nothing on the page says so.** 📌 **History exists — read `build/EMPLOYER_SIGNING_ITEM9_SCOPING_PROMPT.md` and `build/ITEM9_GIFTSIGNING_BUILD_BRIEF.md` before designing a fix.** | 23 Aug 2026 (full-page render) | 🔴 High | Not yet scoped. | 🔑 **23 Aug — KATE FOUND THE MECHANISM, which upgrades this from a layout nicety to a real job. It isn't only the signing block's LAYOUT implying colleagues are the helpers — the COPY has been nudging that way too, through the placeholders that shape what a dozen colleagues actually write (see the care-not-commitment rule on #064 and #065).** ⚖️ **Fixing the placeholders removes the nudge. It does NOT fix the underlying gap: nothing on that page ever says plainly that the people who help are the ones SHE chooses. That line is still owed, and it belongs here.**
| 060 | 🟢 | 🚨🚨 **POSSIBLE MATERNITY-LEAVE COPY ON A BEREAVEMENT PAGE — potentially the most serious bug in this log, and potentially LIVE.** The corrected full-page render, panel labelled `bereavement`, shows the gifter's letter as *"Zara, we wanted to send you off with something more useful than flowers"* and ***"The next few months are going to be wonderful and completely relentless, often in the same hour."*** ❗ ***"Send you off"* is a maternity-leave farewell, and *"the next few months are going to be wonderful"* is being said to someone whose person has just died.** ⚠️ **NOT YET CONFIRMED AS REAL — it may be harness sample data held constant across occasions, which is EXACTLY the shape of the school-pickup false alarm Code caught in its own mock hours earlier. Must be proved, not assumed, in either direction.** 🔑 **Only became visible because the preview was corrected to include the page shell — the 1,800px Code had been omitting. The preview bug is what exposed this.** ❗ **Key question that sets the severity: is the letter ever sent WITHOUT a human writing or approving it? A bad default a buyer edits is a bad default. A default that goes out untouched is a disaster.** | 23 Aug 2026 (full-page render) | 🔴🔴 **CRITICAL — everything stops until it is answered** | Investigation sent. Nothing to be pushed or merged until it reports. | ✅ **STOOD DOWN 23 Aug — FALSE ALARM, and the harness again. The letter was hardcoded in `preview035.tsx:81` and held constant across all five occasions, exactly as the school-pickup task list had been. `isTeamCard` and "The team at Meridian" were the scaffold's too. SECOND false alarm from the same mock in one afternoon.** 🔑 **Code checked its OWN harness first — *"since that's the likeliest answer and the one I got wrong last time"* — which is why this cost twenty minutes instead of a day.** ✅ **The product has NO default letter at all: `gifted_by_note` is optional free text a human types, stored verbatim or null, never generated, never auto-sent, and NOT PRESENT IN ANY EMAIL. No note means no letter block renders.** ✅ **And the email module was already defending against this: bereavement gets *"There are no right words for a time like this…"*, plus a comment stating outright that a gift for illness must never open with new-baby words.** ⚖️ **The scare was false and the discipline still paid — investigating it properly found #062, a real live bug nobody was looking for, with the same consequence by a different route.**
| 059 | 🔴 | **Latent baby copy behind `??` fallbacks — a landmine that fires silently on a bereavement page.** `?? "just welcomed their new baby"` and `?? "getting ready for the new baby"` sit behind NULLABLE fields in `GiftActivation.tsx:308` and `:314`. ✅ **Unreachable today, because `gifts.ts:381` always sends a non-null occasion default.** ⚠️ **But unreachable today is not the same as safe: the day anyone changes that default upstream, a bereaved recipient is told her people will hear she *"just welcomed their new baby"*, with nothing failing and nobody alerted.** ⚖️ **Code's own pick as the one item on its six-string audit genuinely worth worrying about, and it was right. Fix: degrade to the occasion-neutral `other` wording so a null is true for everyone.** | 23 Aug 2026 (occasion sweep) | 🔴 High | Ruled: fix now. |
| 058 | 🔴 | **THERE IS NO `surgery` OCCASION — and Kate's friend, the entire reason this rehearsal exists, is having planned surgery.** The enum is `new_baby | illness_recovery | bereavement | ongoing_support | other`, so planned surgery falls under `illness_recovery`, whose everyday invite line reads *"not been well lately"*. ❗ **That is simply untrue of someone who is perfectly well and having a knee done in three weeks — and it's the line her thirty friends will read.** 🔎 **Someone HAD thought about this once: Code found the TRUSTED line deliberately worded not to assume the procedure has happened. The everyday line never got the same care.** ⚖️ **Not to be fixed one line at a time — the whole `illness_recovery` set (invite lines, task suggestions, activation copy) to be shown end to end and judged against a planned procedure as a whole.** | 23 Aug 2026 (occasion sweep) | 🔴 **High — it is this week's actual use case** | Audit requested. | 🔑 **23 Aug — GROUNDWORK ALREADY EXISTS. Code found a `surgery` case PRE-WIRED in the email module (`email.ts:1196`) for a future enum value that doesn't exist yet. So whoever fixes this is not starting from nothing — the copy branch is written and waiting on the enum.**
| 057 | 🟢 | 🚨 **`/api/healthz` CAN BE SERVED FROM VERCEL'S CACHE — so the one command built to end the deploy guessing can itself return a STALE answer.** 🔎 **Found 23 Aug by Code, and flagged rather than glossed: its FIRST header check on the production domain came back `Server: Vercel` with no `X-Railway-*` headers at all. Re-run cache-busted it returned `X-Vercel-Cache: MISS` and hit Railway's origin. Both gave the same commit this time — but the first check ALONE would not have proved it.** ❗❗ **This is precisely the failure mode #053 exists to prevent. The endpoint's entire value is that a plain one-line `curl` tells the truth; if the edge can cache it, the plain curl can lie, and the workaround (knowing to cache-bust) defeats the point of having a one-line check.** ✅ **Fix is tiny: `Cache-Control: no-store` on that route so it can never be cached at the edge. ~2 minutes.** ⚠️ **Until it lands, `build/TECH_STACK.md` carries the cache-busted form of the command.** | 23 Aug 2026 (post-#69 verification) | 🔴 **High — small, but it protects the thing that protects everything else** | Queued as item 0 of the next Code session. ✅ **FIXED + MERGED + PROVEN ON PROD, 23 Aug (PR #70).** `Cache-Control: no-store` set on the route itself, plus `CDN-Cache-Control`/`Vercel-CDN-Cache-Control` which Vercel honours ahead of `Cache-Control`. 🔎 **The original header was `public, max-age=0, must-revalidate` — cacheable-with-revalidation, so the edge was entitled to answer. Three probe curls all showed `X-Vercel-Cache: MISS`, so a stale HIT was never actually caught; the header GRANTED permission, which is enough to make the check untrustworthy.** 🟢 **Green because the instrument was verified with the very thing it exists to support — a plain `curl -sS -D - https://www.auntlucy.com.au/api/healthz`, no cache-busting — returning `Cache-Control: no-store` and `commit: 9a90032`.** |
| 056 | 🔴 | **The greeting runs straight into the body — *"Hi Priya, Sarah has asked you to help run Mel's page…"* — instead of sitting on its own line like every other email.** ✅ **Kate, 23 Aug: *"this really should have a line break after Hi Priya. Not a screaming priority at this stage."*** 📌 **Affects BOTH emails built by `buildItem17Email` — the access grant AND the task-changed note (*"Hi Jo, small change on Mel's page…"*). One fix, two emails.** ⚖️ **Every other email in the family opens *"Hi Mel,"* on its own line and then a paragraph, so this is a consistency gap rather than a fault — which is exactly why it survived six rounds of looking at these emails.** | 23 Aug 2026 (Outlook check) | 🟢 Low | Kate's call — bundle it into the email copy round (#052, #050) rather than its own job. |
| 055 | 🟢 | **The MAGIC-LINK email's button is STILL broken in Outlook Classic — no padding (the label fills the terracotta box edge to edge), not centred (hard left), and no gap beneath (the next line is jammed against it).** 🔑🔑 **AND THIS IS THE BEST NEWS OF THE DAY, NOT THE WORST: it is the CONTROL GROUP. This email builds its OWN button — it was inlined inside `sendMagicLink` until 23 Aug and never went through `renderButton`, so it never received the #034 hardening. Same client, same engine, same email family: every HARDENED button rendered correctly and the one UNHARDENED button failed in precisely the three ways Code predicted from Word's engine. The diagnosis is now evidenced rather than argued.** ⚖️ **Fix: route it through the shared hardened `renderButton`.** ❓ **Its button is TERRACOTTA, not the green `#2D6A4F` — establish whether that's deliberate or drift before changing any colour.** | 23 Aug 2026 (Outlook Classic check) | 🔴 High | Round 3 → `build/EMAIL_ROUND3_PROMPT.md`. | ✅ **23 Aug — FIXED on branch `claude/email-round-2` by routing the sign-in button through the shared hardened `renderButton`.** ⚖️ **COLOUR RULING: it was DRIFT, not intent. Commit `49957e4` — the approved first-impressions pass — created `renderButton` and in the same commit converted buttons from `#E76F51` to `#2D6A4F`. It converted every button it could reach and simply couldn't reach this one, because this one wasn't a `renderButton` call. The same omission that cost it the #034 hardening cost it the colour. Now green, and green stays.** 📌 **One unavoidable side effect, named by Code rather than hidden: routing through `renderButton` also escapes the magic link into the `href`, which it previously wasn't. Strictly an improvement.**
| 054 | 🟢 | **The MAGIC-LINK email overflows the screen sideways on every phone — 56px at 375px, 83px at 348px, 111px at 320px.** 🔎 **Cause: the footer's raw magic-link URL has no `word-break`, so it props the card open at a hard 399px minimum and the card can never narrow.** ⚠️ **This is the LOGIN email — it looks broken at the exact moment someone is authenticating, which is the worst possible moment for a product asking to be trusted with a friend's hardest weeks.** 🔑 **Found ONLY because we insisted on rendering it rather than waiving the protocol for a "one-word fix" — the second time this pass that rendering first paid for itself. The logo ratio fix (#044) landed but is LATENT until the overflow is fixed: the image never reached `max-width`, so it never got squeezed.** | 23 Aug 2026 (post-merge) | 🔴 High | Logged, deliberately not fixed in PR #66. Own job. | ✅ **23 Aug — SEEN IN THE REAL INBOX and it turns out to be two problems with one fix. In Outlook Classic the fallback URL wraps MID-URL across two lines, which looks broken; and it is the same unbroken URL that props the card open at 399px and overflows a phone by up to 111px.** ⚖️ **Fix both at once: put the URL on its OWN line after the sentence and give it `word-break`/`overflow-wrap`.** ❗ **KEEP the fallback link — Kate asked whether it's needed and today answered her own question: the magic-link BUTTON failed in her client, so the fallback URL was the only way into that email. It earned its place.** ✅✅ **23 Aug — FIXED WITH NUMBERS on branch `claude/email-round-2`: overflow at 320 / 348 / 375px went 111 / 83 / 56px → 0 / 0 / 0.** 🔑 **And a knock-on worth noting: because the card can finally narrow, #044's logo fix on this email STOPS BEING LATENT — the lockup now actually scales (224×55, 252×62, 279×69), correct ratio throughout. A fix that was real but inert is now doing its job.**
| 053 | 🟢 | **NOTHING DOWNSTREAM OF THE RUNNING SERVER EXPOSES WHAT VERSION IT IS SERVING — `/api/healthz` returns a constant `{"status":"ok"}`.** ⚠️ **Not a bug in the product; a hole in the ground under it. Code was straight about the limit of its own verification: *"Railway reports success for this SHA" + "the server is alive" is a deployment record plus a liveness check, NOT a version check.*** 🔑 **THIS GAP IS THE WHOLE WEEK: #042 · the stale local `main` · four different things called "done" · every re-probe that found something. Every one was a version question nobody could answer in under an hour.** ✅ **Fix: Railway injects `RAILWAY_GIT_COMMIT_SHA` — put it in the healthz payload and every future deploy is verifiable in one command, forever. Spec-first: `openapi.yaml` → regenerate → route, per CLAUDE.md.** ❗ **Confirm the variable is actually populated before building around it — an endpoint that reports "unknown" is worse than none.** | 23 Aug 2026 (post-merge) | 🔴 **High — highest-leverage item remaining** | Prioritised ahead of the email copy round. | ✅✅ **FIXED, SHIPPED AND PROVED 23 Aug — PR #67 (`c404034`), merged `0b5386f`. `curl -s https://www.auntlucy.com.au/api/healthz` → `{"status":"ok","commit":"0b5386f70a880edb81b3174a384854a8e1a44f26"}`, byte-identical to `git rev-parse origin/main`.** 🔑 **THE WEEK'S CENTRAL PROBLEM IS NOW A ONE-LINE COMMAND. Pinned permanently in `build/TECH_STACK.md`.** ⚖️ **And Code handled a blocked instruction properly rather than guessing: it could NOT confirm `RAILWAY_GIT_COMMIT_SHA` in advance (the only way to read Railway's env is from inside a deploy — chicken and egg), so instead of assuming it built a three-source fallback, stamped at BUILD time via esbuild `define` — `RAILWAY_GIT_COMMIT_SHA` → `git rev-parse HEAD` → runtime env — returning `null`, never `"unknown"`. Better than what was asked for. The variable turned out to be populated and it resolved first try.**
| 052 | 🔴 | **In the claim-confirmation email the two helper controls — *Add this to your calendar* and *release this slot* — read as body text, not as things you can do.** ✅ **Kate on the 23 Aug side-by-side: the email now "looks much better" after #043, "but it would be good if the Add to your calendar and Release this slot were buttons rather than hyperlinks."** ⚠️⚠️ **THEY MUST NOT GET EQUAL WEIGHT. Un-claiming went live 22 Aug and works. A Release button as inviting as the calendar button is a beautifully designed drop-out control placed in front of every helper — a self-inflicted risk.** ⚖️ **Ruling: calendar = green button (the thing we want) · release = quiet outlined button or a clear text link (findable, not inviting).** 📌 **Needs a small copy reflow, not just styling — lifting "release this slot" out of its sentence collapses it.** | 23 Aug 2026 (side-by-side review) | 🟡 Medium | Copy round 2 → `content/EMAIL_COPY_ROUND2.md`. | 📌 **23 Aug — DELIBERATELY HELD BACK ONE ROUND. The task-changed email (#045) is the same design problem in a simpler setting: one control, no competition. If the quiet-outlined treatment reads right there, apply it here with confidence; if it reads wrong, we learned it on the email that matters less.**
| 051 | 🟢 | **NO EMAIL THE PRODUCT SENDS HAS ANY ROUTE TO THE HOMEPAGE — a helper who has never heard of Aunt Lucy is asked to drop off a meal by a brand they cannot look up.** 💰 **The email twin of #041, and the same commercial argument: ~30 helpers per page, every one of them the ideal buyer, and not one of them is told what this is.** ✅ **Kate, 23 Aug: "all of them should have a line at the bottom that takes them to the Aunt Lucy homepage so they can get an understanding of what this is all about if this is the first time they've heard of it."** ⚖️ **Ruling: the SAME quiet line in every email including the hard ones — explanatory, not promotional, no buy link, no pitch. Same restraint as #041: it must never turn a bereavement email into a shopfront.** ⚠️ **One line, small, unbold, in the existing grey footer band — which already carries a fallback link and sometimes an unsubscribe line. It must not become a footer stack.** | 23 Aug 2026 (side-by-side review) | 🔴 High | Three drafts awaiting Kate's pick → `content/EMAIL_COPY_ROUND2.md`. | ✅ **23 Aug — IMPLEMENTATION DECIDED, and Kate found the neater route herself: rather than bolting on a new element, put ONE identical line in every footer and hyperlink the words "Aunt Lucy" inside it. No button, no "buy one", no pitch — the homepage does the selling. Wording approved: *"New to Aunt Lucy? It's a simple way for friends and family to organise practical help — meals, lifts, the school run — without the person in the thick of it having to ask."*** 💰 **Kate's own framing of why: *"for credibility and also for marketing if they want one of their own"* — the #041 argument, arriving independently.** ⚠️ **The eight footers are currently INCONSISTENT — some carry a fallback link, some an unsubscribe line, one a "you received this because" line. All eight must be shown before and after; three stacked grey lines is a real risk.** 🔎 **23 Aug — NOW RECOGNISED AS A PATTERN, NOT A BUG. The same hole exists on three surfaces: the public support page (#041) · every email (#051) · and the activation page (#035, where Kate raised it independently). Everywhere a stranger meets Aunt Lucy, there is no way to find out what Aunt Lucy is.** ⚖️ **One consistent answer across all three rather than three ad-hoc patches — the words "Aunt Lucy", linked, quietly, once.** ✅ **23 Aug — BUILT on branch `claude/email-round-2` and seen in Outlook Classic: one line in every footer, *"New to Aunt Lucy?"*, with the words "Aunt Lucy" hyperlinked. Code checked first and confirmed every footer previously had exactly ONE line, so this makes two everywhere — never the three-line stack that was feared. The sign-in email is the only three, and deliberately: its fallback URL was split onto its own line for #054, grouped as two blocks not three lines.**
| 050 | 🔴 | **The claim-confirmation email has no preheader — the one email every helper is guaranteed to receive has no controlled inbox preview line.** ✅ **Deliberately left EMPTY during the #043 migration (23 Aug) because writing one is a copy change, not a layout one — correct call by Code.** **Today the preview falls through to *"Hi Jo, Thank you so much for stepping up…"*, which is serviceable but unchosen.** 💰 **Every other email in the family has a written preheader. This is free inbox real estate on the product's widest brand impression.** | 23 Aug 2026 (email chrome Step 2) | 🟢 Low | Copy job — Kate to approve a preheader when the copy pass runs. |
| 049 | 🟢 | **`sendInviteEmail` (`email.ts:~501`) is a SECOND invite-sending path that Code describes as dead — and nobody has proved it.** ⚠️ **"Dead" is an assumption, and this is the week where four different things were called "done" and only one of them was true.** ❗ **Do NOT fix its layout. Fixing dead code makes it look maintained, and the next person wires it up by mistake.** ⚖️ **Two outcomes: if it IS dead it should be deleted or explicitly marked, not beautified. If it is NOT dead, there is a live second invite path nobody knew about, and that is a bug in its own right, not a layout job.** 📌 **Report first, act second.** | 23 Aug 2026 (email chrome Step 2) | 🟡 Medium | Awaiting a caller trace from Code. | ✅ **23 Aug — PROVED DEAD, decisively. Code's evidence: 1 reference in the whole repo (its own declaration) · 0 importers across 11 modules that import `lib/email` · no dynamic dispatch · and the clincher — 0 occurrences in the built bundle `dist/index.mjs`, where `sendHelperInviteEmail` appears 5×, `sendClaimConfirmation` 5× and `sendMagicLink` 3×. esbuild tree-shakes it out entirely; nothing in production can reach it. A leftover from `c5ce0f9`, superseded by `sendHelperInviteEmail`.** ⚖️ **KATE'S RULING: DELETE it along with `InviteEmailParams` — ~50 lines, git keeps the history, and leaving it is a trap because its `padding:20px`-on-a-table reads as current practice to whoever copies it next. Own isolated commit.** ✅ **DELETED 23 Aug (`80f6157`), own isolated commit, with `InviteEmailParams`. Typecheck clean, 58/58 tests, and the re-render reported NO EMAIL CHANGED — the deletion proving itself inert.**
| 048 | 🔴 | 🚩 **`dispatch-invites` CLAIMS ITS WHOLE BATCH BEFORE SENDING — so any uncaught exception in the loop silently loses every invite behind it.** The batch is flipped `queued → sent` up front (`internal.ts:314`) and **there is no try/catch inside the send loop.** **An invite marked `sent` that never sent leaves nothing to retry from and nothing to alert on.** ✅ **Found 23 Aug by Code while designing the #046 guard — it caught the render throw at `sendHelperInviteEmail` so one bad invite can't take out the batch, which fixes THAT case.** ❌ **It does not fix the shape: a Resend timeout, a network blip or a DB error in that loop still loses the remainder, silently.** ⚖️ **Not fixed in the chrome branch — deliberately out of scope. Needs scoping: per-invite try/catch, and whether claim-before-send is the right pattern at all.** | 23 Aug 2026 (email chrome Step 2) | 🔴 High | Scoping requested. **A real find that surfaced inside an unrelated layout pass.** | ✅ **23 Aug — SCOPED by Code and accepted in full. Per-invite try/catch is ~15 lines and reuses the `ok === false` path that already exists (`status: failed`, `sentAt: null`, `failedAt: now`), converting "lose the rest of the batch" into "lose one invite, visibly".** 🔎 **And it surfaced a real split: `dispatch-invites` claims-then-drops (optimising against double-sends), while its sibling `dispatch-claim-notifications` claims-then-REVERTS-on-failure, explicitly reasoning that eventual delivery beats a rare duplicate. Two philosophies, same file, neither decided on purpose.** ⚖️ **Cowork's argument for deciding toward DELIVERY: a duplicate "please help" is embarrassing for four seconds; an invite that silently never sends means the recipient gets less help and nobody ever finds out. The product exists so someone in the thick of it doesn't have to ask — an invite that vanishes quietly is that promise failing invisibly.** 📌 **Target: keep claim-before-send, add the per-invite catch, add a `sending` intermediate state so a hard crash leaves rows visibly stuck rather than falsely `sent`. Separate branch.**
| 047 | 🟢 | **In BOTH invite emails the HTML puts the CTA button BELOW the sign-off, while the plain-text version puts it before — the two disagree in every invite the product sends.** 🔎 **Root cause found 23 Aug (Code, read-only pass): `renderHelperInviteEmailHtml` (`email.ts:597`) splits the body on blank lines, DELETES any paragraph starting `<label> →`, then staples `renderButton()` onto the end. It never injects at the CTA's position.** ⚖️ **The approved copy already puts the CTA before the sign-off, so putting the button back in copy position is NOT a copy change — it is the render finally matching what was approved.** | 23 Aug 2026 (email chrome Step 1) | 🔴 High | 🟡 Fix scoped — Step 2 of the email chrome layout pass. | ✅ **FIXED ON BRANCH 23 Aug (commit `98bd473`): the CTA is now replaced in place rather than filtered out and appended, so the button sits above the sign-off and matches plain-text order. No `.txt` file changed.** 🚦 **NOT deployed.** 🚀 **MERGED AND DEPLOYED 23 Aug — PR #66, merge commit `6afc480`, Railway `success` 01:37:42 UTC.** 🟡 **Stays amber under the new rule: 🟡 = fixed, merged and deployed; 🟢 = someone has looked at the real thing and it was right. Turns green after `research/EMAIL_RENDER_PASS.md`.**
| 046 | 🟢 | **Latent: `ctaLabel` drift will silently double the CTA.** `renderHelperInviteEmailHtml` defaults `ctaLabel` to `"See how you can help"`, and the filter that strips the CTA line from the body is an EXACT `startsWith` match. **If a caller ever omits the label — or the copy gains leading whitespace — the raw `Have a look → https://…` line survives as dead body text (an unescaped URL, not clickable) AND a button with the wrong words is appended below it. Two CTAs, one of them dead.** ✅ **Not firing today: all three callers pass it correctly (`invites.ts:126`, `manage.ts:838`, `internal.ts:389`) — but nothing enforces it.** ⚠️ **Numbered because unnumbered things are invisible.** | 23 Aug 2026 (email chrome Step 1) | 🟡 Medium | Needs a guard, not a fix — fail loudly rather than render two CTAs. | ✅ **GUARDED ON BRANCH 23 Aug (commit `57bbba8`) — and the design mattered more than the guard.** ⚖️ **Code chose THROW at the renderer (so it cannot physically emit two CTAs) plus CATCH at `sendHelperInviteEmail` into `logger.error` + `false`, which the dispatcher already stamps `failed`/`failedAt` — loud, visible, retryable, one invite affected.** 🚩 **The reason the catch is essential is now bug #048: the batch is flipped to `sent` before sending with no try/catch in the loop, so a bare throw would have silently lost every invite behind it.** ✅ **Verified: valid trusted, valid general and a personal-opener body each render exactly one button and no raw URL; a forgotten `ctaLabel` and a leading space both throw.** **Changed the diagnostic render only — no real email moved.** ✅ **23 Aug — VERIFIED BY KATE'S OWN EYES, though she read it as a failure at first: the side-by-side's last panel is a DELIBERATE booby-trap, not a real email. Left pane = the old behaviour with two calls to action, one of them a dead unclickable URL. Right pane = *"Guard fired — this email was refused, not sent"*. The refusal IS the feature.** 📌 **Action for the harness: label that panel far more loudly. An intentionally-broken test case that reads as a broken product is a review trap, and it cost a round-trip.** 🚀 **MERGED AND DEPLOYED 23 Aug — PR #66, merge commit `6afc480`, Railway `success` 01:37:42 UTC.** 🟡 **Stays amber under the new rule: 🟡 = fixed, merged and deployed; 🟢 = someone has looked at the real thing and it was right. Turns green after `research/EMAIL_RENDER_PASS.md`.** 🟢 **CLOSED 23 Aug. And the guard got a guard: Code sabotaged it FOUR ways to prove it fails when it should — dropping the colour from the inner `<span>` · removing the MSO block from all three heads · generating head rules for one variant while another exists · and adding a whole new variant to confirm it's covered automatically.** ❗ **The FIRST sabotage PASSED when it shouldn't have — because the sabotage itself was wrong (three `<head>` elements, only one patched). The guard was right; the test OF the guard was broken.** 🔑 **A test you haven't tried to break is not evidence — it's a belief with a green tick next to it.**
| 045 | 🟢 | **The task-changed and access-grant emails have NO button at all — their only action is a raw URL buried mid-sentence.** Both route through `buildItem17Email` (`email.ts:1469`), which linkifies a bare URL inside a paragraph and never calls `renderButton()`. **Task-changed renders as a single dense paragraph.** ⚖️ **This is a COPY job, not a layout one: the body reads *"If it doesn't: <url> — one tap, no drama"*, so lifting the URL into a button leaves a dangling colon. There is no primary CTA to promote — one has to be written, in Kate's voice.** 📌 **Interim for Step 2 (layout only, zero words changed): style the inline URL as a proper link.** | 23 Aug 2026 (email chrome Step 1) | 🟡 Medium | Interim inline-link styling in Step 2; real CTA copy deferred to the copy pass. | ✅ **INTERIM DONE ON BRANCH 23 Aug (commit `6b127fd`): the inline URL was already an anchor but undersized and didn't read as a control — restyled, zero words changed. Touched emails 4 and 7 ONLY, which independently confirms those are the only two button-less emails.** ❗ **The real CTA copy is still owed and this bug stays OPEN for the copy pass.** ✅ **23 Aug — KATE HAS NOW SPECIFIED BOTH MISSING CTAs, which unblocks this.** **Task-changed → a QUIET outlined `Release this slot`, and deliberately NO green button: the email's whole point is that nothing is needed from the helper, so an email whose only control is the escape hatch is telling the truth about what it wants.** **Access-grant → a GREEN `Open Mel's page`, with the plain URL kept underneath (the copy calls it *"your own private link"*, and people forward, bookmark and screenshot links in a way they cannot do with a button).** ❗ **BLOCKING CHECK before building the task-changed CTA: Kate asked "or is there a change option?" — confirm a helper genuinely CANNOT edit a task (only owner and co-managers can, per #040). If a helper can, this entire email is wrong.** 🚀 **MERGED AND DEPLOYED 23 Aug — PR #66, merge commit `6afc480`, Railway `success` 01:37:42 UTC.** 🟡 **Stays amber under the new rule: 🟡 = fixed, merged and deployed; 🟢 = someone has looked at the real thing and it was right. Turns green after `research/EMAIL_RENDER_PASS.md`.** ✅ **23 Aug — RE-CONFIRMED BY KATE FROM THE REAL INBOX, unprompted, on both halves: *"On 4, should Release be a button or at least a hyperlink"* and *"On 7 it should have a button for the Manage link."* Same conclusion she reached from the browser side-by-side, reached again from a live email — copy approved and building in round 3.** ❗❗ **23 Aug — COWORK'S ASSUMPTION WAS WRONG AND THE BLOCKING CHECK CAUGHT IT. Cowork ruled a helper *"almost certainly cannot change a task, only release it"*. Not true: `/release/:token` is a MANAGE-YOUR-CLAIM page — release · reschedule (FLEXIBLE tasks only; fixed returns 409 *"This task's time is set by the family"*) · leave a note (≤200 chars, recipient-only). The GET even returns `flexibility` so the page can choose its controls.** ⚠️ **A `[Release this slot]` button would have told a helper who just needed a different hour that their only option was to hand the task back — worse for them AND worse for the recipient, since the slot returns to the list instead of shifting.** ⚖️ **RESOLVED with a NEUTRAL label — `[ Sort it out here ]`, quiet not green — for a reason worth keeping: THE DESTINATION PAGE IS ALREADY CONDITIONAL, so a conditional email would put the same rule in two places, and the email is the harder one to keep correct. Delegate to the page.** ✅ **Access-grant half BUILT (green `Open Mel's page`).** ⚠️ **23 Aug, ROUND 4 OUTCOME — SEVEN OF EIGHT PASSED IN OUTLOOK CLASSIC (Kate: *"All looks good except this one"*). Only task-changed is wrong, in two ways.** 🟣 **(a) The button label renders PURPLE — Outlook's default VISITED-LINK colour, i.e. no colour was forced on the anchor. THIRD colour leak of the weekend and the same shape each time: the primary forces white on BOTH the `<a>` and the inner `<span>`, and each NEW button variant has to be told separately. Fix as a RULE, not a patch.** ✂️ **(b) The sentence is broken — and that one is COWORK'S FAULT, not Code's. The round-4 prompt handed over `If it doesn't: [ Sort it out here ] — one tap, no drama.`, embedding a BLOCK-level button inside a sentence. Code rendered exactly what it was given: a fragment, a button, then an orphan opening with an em dash. Rewritten as three clean blocks — paragraph, paragraph, button, nothing after it → `build/EMAIL_ROUND5_PROMPT.md`.** ✅ **23 Aug — BOTH FAULTS FIXED, PR #68 open. And COWORK'S DIAGNOSIS OF THE PURPLE LABEL WAS WRONG in a way worth recording. Cowork said the quiet variant had never been given the forced-colour treatment. It had — both variants force colour on the `<a>` AND the inner `<span>`, byte for byte identically.** 🔑 **The real cause is the VISITED state: per-URL, not per-variant, and inline styles CANNOT express `:visited` at all. Word applies its own FollowedHyperlink colour; inline styling only ever wins the unvisited case. The release link was simply the one Kate had already clicked.** ❗❗ **So the GREEN PRIMARY was exposed to exactly the same failure and had only been lucky — and Cowork's patch would have made the quiet button look fixed while leaving the primary to fail later, on a link already clicked, in an inbox nobody was watching. A right-looking fix aimed at the wrong cause is worse than no fix, because it stops anyone looking.** ✅ **Properly fixed for BOTH with an MSO-conditional `<style>` block covering `:link/:visited/:hover/:active`, scoped to the button classes so body links keep their own colours, invisible to every non-Outlook client, and generated from the same variant table so a NEW variant gets its rule by existing.** 📌 **Copy rebuilt as three blocks; plain text identical in the same order. And Code measured something nobody asked for: this body doubles as the SMS — 4 segments before, 4 after (219 → 238 chars, UCS-2). It would have been found in a phone bill.** ✅ **23 Aug — CONFIRMED FIXED IN OUTLOOK CLASSIC. Kate: *"It's not purple anymore."* Label green, sentence complete before the button, nothing stranded after it.** ⚖️ **ONE PREFERENCE, AND COWORK'S QUIET-BUTTON RULE LOSES IT: Kate wants the SOLID GREEN treatment to match the family. The "quiet escape hatch" argument holds where a quiet control sits NEXT TO a green one and loses the comparison — that's #052, the claim confirmation. HERE it is the only control in the email, so there is nothing for it to compete with. Kate's right.** 📌 **One line, riding along with the hardening PR rather than becoming its own round. The outlined variant STAYS in the code — #052 will want it.**
| 044 | 🟢 | **The Aunt Lucy logo STRETCHES vertically on narrow screens — in all six chrome emails.** The lockup is set `width:280px; height:69px; max-width:100%`: the width shrinks with the viewport, the fixed height does not. **Measured 11% vertical stretch at a 348px viewport, worse below.** ⚠️ **Nobody had this on any list, and most email is opened on a phone — so it is plausibly the most-SEEN defect of the whole set.** ✅ **One line in `renderGiftLayout` (`email.ts:845`); zero risk; do it first.** | 23 Aug 2026 (email chrome Step 1) | 🔴 High | 🟡 Fix scoped — Step 2, commit 1. | ✅ **FIXED ON BRANCH `claude/email-chrome-layout` 23 Aug (commit `8bd78bd`), one line: `height:auto`. Measured stretch at 320/348/375px went 10.9% / 10.9% / 0.1% → 0 / 0 / 0%. `width`/`height` attributes kept for Word, which lays the card out at a fixed 520px and never narrows.** ⚠️ **The SAME defect exists in the magic-link email (`email.ts:272`) — being folded into the same branch and added to the render harness as a ninth email.** 🚦 **NOT deployed. Do not mark green until a real send is checked.** 🚀 **MERGED AND DEPLOYED 23 Aug — PR #66, merge commit `6afc480`, Railway `success` 01:37:42 UTC.** 🟡 **Stays amber under the new rule: 🟡 = fixed, merged and deployed; 🟢 = someone has looked at the real thing and it was right. Turns green after `research/EMAIL_RENDER_PASS.md`.**
| 043 | 🟢 | **The helper's claim-confirmation email is entirely OFF-BRAND — it looks like a different product.** Green `#7C9A72` header band instead of terracotta · the words "Aunt Lucy" as an `<h1>` instead of the reversed lockup · page background `#FAF9F6` not `#FAF7F2` · green links · **no preheader**. 🔎 **Root cause: it is built by `buildHtml` (`email.ts:88`), a standalone full-document template that predates `renderGiftLayout` and was never migrated onto it. Same root cause as #038.** 💰 **Commercial weight: this is the ONE email every single helper is guaranteed to receive — and helpers are the ideal buyers (see #041). It is the product's widest brand impression, and it is the only email that is off-brand.** | 23 Aug 2026 (email chrome Step 1) | 🔴 High | 🟡 Fix scoped — Step 2, migrate onto `renderGiftLayout`. Highest reward and highest risk in the pass. | ✅ **MIGRATED ON BRANCH 23 Aug (commit `cade259`) — assessed as a clean re-homing, no content reshaped. After: page `#FAF7F2`, band `#E76F51`, correct lockup aspect, links and sign-off `#2D6A4F`, footer verbatim. No `#7C9A72` left in the file. All eight plain-text parts byte-identical to baseline.** 📌 **Preheader deliberately left EMPTY — writing one is a copy change; now logged as #050.** 🚦 **NOT deployed.** 🚀 **MERGED AND DEPLOYED 23 Aug — PR #66, merge commit `6afc480`, Railway `success` 01:37:42 UTC.** 🟡 **Stays amber under the new rule: 🟡 = fixed, merged and deployed; 🟢 = someone has looked at the real thing and it was right. Turns green after `research/EMAIL_RENDER_PASS.md`.**
| 042 | 🟢 | 🚨 **FRONTEND-ONLY CHANGES DO NOT DEPLOY. Vercel silently SKIPS them — and has done for weeks.** The Vercel project is rooted at `artifacts/api-server` (where `vercel.json` lives) and builds the frontend via a cross-package command — **but `@workspace/rally` is NOT declared in api-server's `package.json`.** So Vercel's automatic Ignored Build Step sees no change under the root directory or its declared dependencies, marks the commit **"Skipped — Not affected"**, and never builds. **Correlation exact across the last 12 merges: every PR touching api-server built; every frontend-only PR (#56, #64, #65) was skipped.** ⚠️⚠️ **The failure mode is worse than a plain failure: because the build compiles the frontend FROM CURRENT SOURCE, an unrelated backend deploy later sweeps up whatever frontend commits have piled up — so the site heals at a random moment tied to unrelated work. PR #56 shipped that way, silently, weeks late.** ✅ **Proved against the live bundle: the old #024 copy present, both #64 and #65 strings absent — live was the source tree as of `c2d8a02`.** ⚖️ **Fix: Vercel → Settings → Git → Ignored Build Step → Custom → `exit 1` (in Vercel's semantics exit 0 = skip, exit 1 = build), forcing every push to `main` to build.** ⚠️ **Do NOT "fix the dependency graph" by adding `@workspace/rally` to api-server's dependencies — Railway builds that same package, so it risks dragging the whole frontend into the backend's install to fix a Vercel problem.** | 22 Aug 2026 (found because the gift rehearsal's fixes didn't appear on the live site) | 🔺 Critical | ✅✅ **CLOSED 23 Aug.** **Root cause was a purpose-built Vercel toggle — *"Skip deployments when there are no changes to the root directory or its dependencies"* — which was ENABLED. Now disabled.** ⚖️ **Better fix than the `exit 1` override Cowork endorsed: same intent, precisely targeted, and it leaves the Ignored Build Step free for its real purpose.** ✅ **`17d6ace` redeployed (Ready in 19s) and VERIFIED against the live bundle** (`index-DKBv1wIC.js` → `index-Bw51giVd.js`): old #024 copy **gone**, both #64 and #65 strings **present**, `X-Robots-Tag: noindex` still applied so the `vercel.json` headers survived. ⚠️ **Left deliberately untouched: *"Include files outside the root directory in the Build Step"* stays ENABLED — it's what lets the api-server-rooted build reach across and compile the frontend.**
| 041 | 🔴 | **The public support page is a dead end — no way to reach the Aunt Lucy homepage, and no footer at all.** No link to find out what Aunt Lucy is, **no way to buy one**, no Privacy Policy, no Terms, **no support email**. 💰 **Commercially this is the biggest miss in the product: ~30 helpers see each page, and every one of them is EXACTLY the person who would buy one — a friend of someone going through something hard, looking at proof that it works. That is the cheapest growth loop Aunt Lucy has, and it does not exist.** ⚖️ **Also a compliance gap: the Privacy Policy went live in PR #44 but isn't linked from the pages where members of the public actually enter their name and contact details.** ✅ **And a support email is needed — `hello@auntlucy.com.au` already forwards to Kate's inbox; a helper with a problem currently has nobody to contact.** ⚠️ **Design care needed: the footer must not turn a bereavement page into a shopfront. Quiet, below the fold, no selling.** | 22 Aug 2026 (gift rehearsal) | 🔴 High |
| 040 | 🔴 | **The "Your people" page is overloaded and hard to read.** Kate: *"ugly and confusing. Too much stuff on there."* It carries claims, tasks, contact details, pronoun settings, invite wording, add-a-person **and** co-manager admin on one screen. ⚠️ **And task editing lives ONLY here — not on the task page, where anyone would look for it.** ⚖️ Kate's steer: this page should be about **adding your people**, nothing else. | 22 Aug 2026 (gift rehearsal) | 🔴 High |
| 039 | 🔴 | 🚩 **REGRESSION FROM PR #58 (same day): *"Zara has asked you to help run Zara's Aunt Lucy page."*** **When the RECIPIENT is the one granting access, the granter name resolves to her own name, so it doubles and reads wrong.** ⚠️ **Direct consequence of a decision taken 22 Aug — resolve the granter from `page.recipientName` when the granting grant is the recipient's own — which was right for the common case (the recipient adding her sister, seen by the sister) and wrong for the self-granting case.** ✅ **Fix: when granter and recipient are the same person, don't repeat the name — *"Zara has asked you to help run her page."*** | 22 Aug 2026 (gift rehearsal) | 🔴 High | ⚠️❗ **23 Aug — CANNOT BE TESTED BY THE 23 Aug TEST-SEND SCRIPT, and this is a trap worth naming. The script hard-codes the access-grant BODY string, because the real `managerAccessEmail()` needs a database connection and the script deliberately won't open one against prod Neon. #039 is a NAME-RESOLUTION fault that only appears with live data — so a clean-looking access-grant test email proves nothing about it.** ❗ **Do not sign off email 7 without a REAL co-manager add.** 🔑 **23 Aug — THE CORRECT PATTERN ALREADY EXISTS IN THE CODEBASE, found while fixing #035. `/manage/:token` derives `role: recipient | manager` SERVER-SIDE from the grant behind the token being used (`manage.ts:78` — `const { pageId, grantId, grantRole } = req`), so it genuinely knows who is holding the link. That is the exact OPPOSITE of this bug, where a name is pulled from the wrong side of the relationship.** ⚖️ **When #039 is fixed, follow that pattern rather than inventing one.**
| 038 | 🟢 | **The helper's claim-confirmation email looks broken** — a grey slab with huge vertical gaps between What / When / Feeding / Dietary needs. **The content is correct; the layout reads as a rendering fault.** 📌 **Part of the same email-chrome problem as #034 and the bare task-change email — one design pass fixes all three.** | 22 Aug 2026 (gift rehearsal) | 🔴 High | 🔎 **23 Aug root cause: the field block is a `<table>` with `padding:8px 0` on every cell (`email.ts:161`). 8px bottom + 8px top = a 16px gap between adjacent 18px text lines, on 14px type — measured 265px of block carrying 127px of text, so 52% of it is whitespace. Nothing collapses margins in a table.** ⚠️ **Outlook compounds it: `padding:20px` and `border-radius:8px` sit on the `<table>` element and Word's engine honours neither, so Outlook loses the inset entirely (text flush to the grey edge) while keeping every 16px gap. Browser preview and Outlook will NOT agree — fix both in one job.** 📌 **Same template as #043.** ✅ **FIXED ON BRANCH 23 Aug (commit `60336ca`): padding, background and the bottom gap all moved off the `<table>` onto a `<td>` and a spacer row, because Word honours none of the three on a table. Block 265px/127px text (52% whitespace) → 214px/128px (40%); adjacent-line gaps 16px → 7.6px.** ⚠️ **The same slab exists in the internal pilot-application alert (`email.ts:~369`) — folded in — and in `sendInviteEmail` (`email.ts:~501`), which is DELIBERATELY NOT being fixed; see #049.** 🚦 **NOT deployed.** 🚀 **MERGED AND DEPLOYED 23 Aug — PR #66, merge commit `6afc480`, Railway `success` 01:37:42 UTC.** 🟡 **Stays amber under the new rule: 🟡 = fixed, merged and deployed; 🟢 = someone has looked at the real thing and it was right. Turns green after `research/EMAIL_RENDER_PASS.md`.**
| 037 | 🔴 | **The calendar feed fails to add in Outlook Classic** — *"We can't add the calendar for  right now. Please try again later."* 🔎 **Note the DOUBLE SPACE after "for" — that reads like an empty calendar NAME being interpolated, i.e. a missing `X-WR-CALNAME` in the `.ics`.** ⚠️ **Check that before concluding Outlook is at fault.** Affects the calendar link shipped in PR #55 and now also surfaced on the release page. | 22 Aug 2026 (gift rehearsal) | 🔴 High |
| 036 | 🔴 | **The "It's live" screen shows TWO links and never says which is which.** Kate: *"I thought the bottom one was to ask people to help, but it was another link for adding their supporters."* ⚠️ **She built the product and still got it wrong.** ✅ **They must stay separate — the top link is PUBLIC (safe to send to thirty people); the bottom is the PRIVATE management key (anyone holding it can run the page). The bug is that nothing on screen distinguishes them.** | 22 Aug 2026 (gift rehearsal) | 🔴 High | 🔗 **23 Aug — CONNECTED TO #035, and the new activation copy sharpens it. The approved opener promises *"then share ONE link"*. If the "It's live" screen still presents TWO with nothing saying which is which, the contradiction gets LOUDER, not quieter.** ✅ **The upside: setting the one-link expectation earlier is also what makes that screen readable when she reaches it — so #035 has already done half of #036's work.** 🔎 **23 Aug — COWORK'S BRIEF WAS WRONG AND CODE READ THE ACTUAL SCREEN. The links are NOT unlabelled: there IS prose above each (*"Share this link with anyone who's offered to help"* / *"This is your private link; keep it handy"*). Cowork had drafted replacement prose for both, which would have rewritten words that were already doing their job.** ✅ **The real fault: both render as an IDENTICAL white pill with an IDENTICAL Copy button ~200px apart, and the prose sits far enough above each pill to DETACH from it under a skim. A proximity and symmetry problem, not a missing-words one — so the fix is a two-word label attached to each pill plus breaking the visual symmetry, which is cheaper than the rewrite.** 📌 **Sixth time in one day that reading the thing beat reading the description of it. Revised prompt → `build/BUG036_LINKS_PROMPT.md`.** ✅ **23 Aug — AND THE ESCALATED QUESTION IS ANSWERED WELL: a grieving person does NOT see "Has the baby arrived yet?". Gated on `occasion === "new_baby"` in both the activation page and `/manage`, and verified by RENDERING all five occasions and probing the live DOM rather than by reading the code and trusting it.**
| 035 | 🟡 | **The activation page never says what Aunt Lucy IS.** *"Here's what we thought might help"* jumps straight to a task list — **a recipient who has never heard of Aunt Lucy is asked to curate something before they know what it does.** ⚖️ **This is an activation-conversion problem, not a copy nicety: every number in the business sits downstream of the recipient switching the page on.** ✅ **Kate's own framing is the fix and beats anything in the copy bank: *Aunt Lucy answers the question friends and family always ask — "what can I do to help?" — by letting them claim the things that would actually help.*** | 22 Aug 2026 (gift rehearsal) | 🔴 High | ✅ **COPY APPROVED BY KATE 23 Aug → `build/BUG035_ACTIVATION_COPY.md`, build prompt → `build/BUG035_BUILD_PROMPT.md`.** **Kate rewrote the draft and improved it in three ways: *"approve a short list"* rather than *"keep"* (it tells her someone else did the work and she's only saying yes — lowering effort at the moment she has none) · the barrier named as HERS rather than the helpers' · and she caught a hole nobody else had — without *"then share one link"*, *"your people pick what suits them"* is magic, because nothing explains how they'd ever know.** 🔗 **She also overturned the draft's *"no links at all"* constraint, and was right: *"I'd want to take a look at what I'm signing up for."* She is about to publish a page about her hardest weeks and hand over her friends' contact details — checking who she's trusting is DUE DILIGENCE, not curiosity.** ⚠️ **Implemented as the words "Aunt Lucy" linked ONCE, first mention, opening in a NEW TAB — because this is the highest-stakes conversion moment in the product and a link that navigates away is a leak.** ⏭️ **Sequenced AFTER the email round-3 merge — same folder, same checkout, two Code sessions would fight over branches.** ✅ **23 Aug — BUILT on `claude/bug-035-activation-opener`, copy verbatim, link verified in the DOM as one instance, `href="/"`, `target="_blank"`, `rel="noopener noreferrer"`.** 🔎 **Read-only pass found two things worth keeping: the component is used in exactly ONE place (`/gift/:giftId`), so the explainer can't leak onto a screen it doesn't belong on; and there is NO `/activate` route despite `CLAUDE.md` mentioning one.** ⚖️ **Fix 3c turned out NOT to be on this screen at all — the activation page already said *"Refer to you as"*; the faulty string was on `/manage/:token` and was fixed there, gated on `role === "recipient"` so a sister running the page still correctly sees *"Refer to Zara as"*.** ✍️ **And Code flagged that it had written ONE line of unapproved copy rather than slipping it through: the *"Good to know"* placeholder, whose original ended *"saves waking the baby"* — too baby-specific to leave in a string it had just been told to de-baby. Its replacement was approved as written: *"For example… a quick text before you come is easier than a knock at the door. Doorstep drop-offs are always perfect."*** 🚨 **23 Aug — THE PREVIEW KATE SIGNED OFF WAS NOT THE PAGE. Code mounted `GiftActivation` inside two HAND-COPIED wrapper divs rather than inside `GiftExperience`, silently dropping everything above and below it. The preview was 2,605px; the real page is 4,426px — roughly 1,800px omitted, INCLUDING THE ENTIRE IDENTITY: the teacup mark, *"A little something for you"*, the recipient's name as the `<h1>`, the gifter's letter, the colleague notes, and a *"Made with care · Aunt Lucy"* footer.** ⚖️ **So the page DOES identify itself — Kate's header question is answered — but she judged the opener's PLACEMENT from a picture missing 1,800px above it, and that judgement has to be made again.** 🔑 **Code found and fixed a SECOND harness fault while re-rendering: the colleague notes captured blank because `captureBeyondViewport` paints offscreen content without firing `IntersectionObserver`, so framer-motion's `whileInView` never revealed them. The harness now step-scrolls and asserts the shell is present on every capture.** 📌 **RULE: a preview that omits the page shell is not a preview of the page.** ✅ **MERGED 23 Aug (PR #71) and deployed** — opener + link-in-new-tab + all four smaller fixes. 🟡 **STAYS AMBER: verified only in a local render of the real `GiftExperience` (shell included) at 375px/1280px and swept across all five occasions. NOBODY HAS OPENED A REAL GIFT LINK ON PROD.** 📌 **To go green: open a live gift and read it on a phone.** ⚠️ **Also shipped: `/manage` now says *"Refer to you as"* when the token's own grant is `recipient` (a nominated manager still sees the name) — that string was on `Manage.tsx`, NOT the activation page as originally reported.** |
| 034 | 🟢 | **The "Take a look" button renders MID-PARAGRAPH in the recipient gift email**, jammed between *"...who your people are."* and *"No rush."*, overlapping the text. **It looks broken.** ⚠️ **Distinct from — and worse than — the known "button sits below the sign-off" chrome quirk: this one lands inside the body copy of the single most important email the product sends.** | 22 Aug 2026 (gift rehearsal) | 🔴 High | ⚠️🔺 **ESCALATED 22 Aug: the same broken button renders in a SECOND email — the claim notification to the recipient (*"See who's helping"*). This is SYSTEMIC CHROME, not one bad template.** ⚠️🔎 **23 Aug — NOT REPRODUCED IN SOURCE. Code rendered the gift delivery and the claim notification from the real templates: the button sits correctly between *"…who your people are."* and *"No rush."*, centred and spaced. `renderGiftLayout` is clean, and both #034 emails use it.** ⚖️ **So what Kate saw was almost certainly her EMAIL CLIENT, not the HTML — which fits: #037 was also found in Outlook Classic, whose Word engine honours neither table `padding` nor `border-radius`. NEEDED FROM KATE: which client and device showed the overlap.** ❗ **Do NOT close #034 on the strength of a browser render — that is precisely the mistake this week keeps teaching. #047 is a REAL and DIFFERENT CTA bug (invites only); it is not this one.** 🔧 **23 Aug — HARDENED, NOT FIXED, and deliberately still open (commit `60336ca`). Code named three Word-engine mechanisms in the old markup, in order of likelihood: (1) `margin:0 auto 24px` on a `<table>` — Word ignores table margins outright, which silently deleted the ENTIRE 24px gap below the button AND its centring; (2) `display:inline-block` on the `<a>` — Word has no inline-block, so the anchor stayed inline and its `padding:14px 30px` painted the background OUTSIDE the line box, over the lines above and below (literal overlap, not a gap problem); (3) unitless `line-height:1.2`, which Word resolves unreliably and can clip.** ✅ **Hardened: padding onto the `<td>`, anchor `display:block`, `align="center"` AND `margin:0 auto`, a 24px spacer ROW, `line-height:20px` with `mso-line-height-rule:exactly`. Colour untouched. Browser-identical (141.1×47.2 → 141.1×48.0, same centre, same 24px gap) — the change only alters what Word does.** ❗❗ **NOBODY HAS OPENED THIS IN OUTLOOK. Three things to check when someone does: is there a clear gap under the green button · is it centred · does the label sit INSIDE the green rather than on top of the text above. Until then #034 is reasoning, not a fix.** ⚖️ **23 Aug — KATE'S RULING: MOVE IT (answer B). The button goes BELOW *"No rush. It'll be here when you need it."* and above the sign-off. Position only, no words — folded into the layout branch before the merge, not the copy round.** 🔑 **This settles what #034 actually was: PART rendering fault, PART layout judgment. The render was correct and Kate still didn't like where the button sat — and she was right, because the claim notification email (which she approved) ALREADY does it this way. The gift delivery was the outlier, not the standard.** ❗ **The Outlook half of #034 is still unverified and this stays OPEN.** 🚀 **23 Aug — LAYOUT HALF SHIPPED in PR #66 (`0639f29`): the button now sits below *"No rush. It'll be here when you need it."*. Code confirmed the safety check first — the two emails share `renderButton`'s MARKUP but never its POSITION, each hand-writing its own `contentHtml`, so the claim notification came out byte-identical and the button Kate approved did not move.** ❗❗ **THE OUTLOOK HALF IS STILL UNVERIFIED AND THIS STAYS OPEN. It closes only when a real email is opened in the client where Kate saw the overlap — see `research/EMAIL_RENDER_PASS.md`.** ✅ **23 Aug — CLIENT CONFIRMED AT LAST: Kate uses OUTLOOK CLASSIC, on PC and on mobile. The desktop is the WORD engine, which is exactly the family of faults Code named — so the diagnosis is now testable rather than theoretical.** 🔑 **CRITICAL DISTINCTION FOR READING THE RESULT: Outlook Classic on Windows uses the Word engine; Outlook on the PHONE does NOT — it renders with a normal modern browser engine. So *fixed on PC and fine on phone* = #034 closed and the diagnosis was right. But *still wrong ON THE PHONE* means it was NEVER a Word problem and the whole hypothesis reopens. THE TWO DEVICES MUST BE RECORDED SEPARATELY.** ⚡ **And it does not need a purchase run: all nine emails already render from the real builders, so a throwaway script posting them through the real Resend path settles this in ten minutes → `build/EMAIL_OUTLOOK_TEST_PROMPT.md`.** 📌 **If it IS still wrong on the desktop, the known last resort is a VML roundrect button inside an MSO conditional with the current HTML button as the non-Outlook fallback — Word has never handled padding on an anchor reliably.** ✅✅ **23 Aug — VERIFIED IN OUTLOOK CLASSIC, PC AND MOBILE. Kate: *"it looks much better."* The hardened buttons render correctly in the exact client where the fault was found.** 🔑 **And the verification came with an accidental control group — see #055: the magic-link email builds its own button, never went through `renderButton`, never got the hardening, and failed in precisely the three predicted ways in the same inbox on the same day. Hardened works, unhardened doesn't. That is as close to a controlled experiment as this ever gets.** ⚠️ **NOT YET CLOSED, and for an honest reason: the four emails Kate screenshotted (2, 4, 7, 8) are the four that DON'T demonstrate the fix — three have no green button at all and the fourth is the unhardened one. Needs two minutes on ONE of emails 1, 3, 5 or 6 with the three questions, so it closes on evidence rather than inference.** 🟢🟢 **CLOSED 23 Aug — verified in OUTLOOK CLASSIC on PC and mobile across two separate test sends.** 🔑 **AND IT TURNED OUT TO BE TWO FAULTS WEARING ONE BUG NUMBER: (1) a genuine Word-engine rendering fault — `display:inline-block` on an anchor painting its padded background OVER the lines above and below, table `margin` silently ignored so the 24px gap AND the centring vanished, unitless `line-height` clipping; and (2) a LAYOUT JUDGMENT — the button sat before the closing reassurance instead of after it.** ⚖️ **The second was NEVER visible in a browser and would never have been found by any amount of rendering. It surfaced only because Kate kept saying the position looked wrong AFTER the render had been proved correct — and she was right: the claim notification she'd approved already did it the other way round, so the gift email was the outlier.** 📌 **Lesson: a correct render is not the same as a right one.**
| 033 | 🔴 | **A dated task has no TIME field, and a lift has no one-way/return option.** *"A lift to an appointment"* accepts a date and nothing else — **but a lift without a time is unusable, and whether the helper waits and brings them home is the whole difference between a 20-minute favour and a half-day.** Also applies to *"Add something else"*, which offers even less. ⚠️ **Note the irony: bug #005 (school-pickup TIME) was fixed in August for exactly this reason; the same gap survives on lifts.** | 22 Aug 2026 (gift rehearsal) | 🔴 High |
| 032 | 🟢 | **`prepareInvite` downgrades an email contact's TRUSTED invite to a general one — so that person still can't claim the task they were asked about.** Distinct from bug #031 in mechanism (it sets `slotId = null`, so it fails closed cleanly with no orphan row) but **identical in harm: the organiser picks someone for the school run, and that person cannot pick it up.** 🔑 **ROOT CAUSE IDENTIFIED 22 Aug: there is no approved trusted-invite EMAIL template — the trusted copy is SMS-only by design, so the code downgrades because it has nothing else to send.** ✅ **Draft written (`COPY_BANK.md.md` → "Trusted-invite EMAIL"), awaiting Kate. Approve it and the downgrade stops being necessary — one copy approval closes a code bug.** | 22 Aug 2026 (Claude Code, while fixing bug #031) | 🔴 High | **✅ CLOSED — merged and verifiable on `main`** (confirmed by the 22 Aug banked-vs-shipped audit; the row had been left open by Cowork's bookkeeping, not by the work).
| 031 | 🟢 | **A trusted-slot invite sent by EMAIL produces a task the invitee cannot claim — silently.** `invites.ts:82` collapses two decisions into one ternary (`kind: contactIsEmail ? "general" : "trusted"`), so inviting someone to a `trustedHelpersOnly` slot with an email address mints **no `inviteToken`** and links to `/s/:slug`. **But trusted slots are hidden from the public listing and 404 on the public claim path (`slots.ts:79`) — so the helper CANNOT claim the task they were personally invited to, and nobody is told.** Leaves an inconsistent row (`slotId` set, `kind: "general"`, null token). ⚠️ **Fails closed, but the intent is silently broken: the organiser hand-picks someone for the school run, and the task simply never gets picked up.** ⚠️ **Rare on the SMS-first crisis path; COMMON on the gift path, where people mostly type email addresses.** ✅ **Fix already exists in the same codebase — `prepareInvite` (`manage.ts:608`) keeps kind and channel as separate variables. Ruling + prompt: `build/BUG031_TRUSTED_INVITE_RULING.md`.** | 22 Aug 2026 (Claude Code, while answering Question 0) | 🔴 High | ✅ **PRODUCTION AUDIT CLEAN — Kate ran it herself in the Neon SQL editor, 22 Aug: ZERO rows.** **No real person has ever been invited to a trusted task by email, so nobody is sitting on an invitation they cannot act on. Nothing to repair, no one to contact.** ⚠️ **A live trap, fixed before it caught anyone.** **✅ CLOSED — merged as PR #62 and verifiable on `main`** (confirmed by the 22 Aug banked-vs-shipped audit; the production audit had already returned zero affected rows).
| 030 | 🟢 | **⚠️ UNCONFIRMED, AND IT OUTRANKS EVERYTHING IF TRUE: is "trusted" being decided by CONTACT FORMAT?** Claude Code's #013 investigation stated that `invites.ts:72` *"routes non-email contacts to `kind: \"trusted\"`"*. **Two readings: (a) the organiser marks someone trusted and trusted invites are simply DELIVERED by SMS — unremarkable; or (b) a helper given a phone number is CLASSIFIED as trusted.** ⚠️ **"Trusted" in Aunt Lucy means *can be given the sensitive jobs* — minding the baby, collecting the kids. If (b), the safeguarding boundary of the product is being set by whether the organiser typed a phone number instead of an email address.** **Most likely loose phrasing for (a). MUST be confirmed before any #013 code is written — Question 0 in `build/BUG013_ANSWERS.md`.** | 22 Aug 2026 (raised by Cowork from Code's #013 investigation) | 🔺 Confirm first | **✅ CLOSED 22 Aug 2026 — IT IS (a). NOT a safeguarding problem.** `manage.ts:608` proves the model: `channel` from contact format, `kind` from whether a slot was chosen. **`contacts.trusted` is never touched by the organiser path — nobody is tagged trusted anywhere persistent.** ⚠️ **Raising it was still right: a sentence that could be read as "phone number ⇒ may collect your children" has to be checked, not assumed away — and the check cost twenty minutes and turned up bug #031 next door.**
| 029 | 🟢 | **The trusted-invite claim path sends NOTHING — no email, no SMS — and its cohort is structurally phone-only.** `invites.ts:215` returns `cancelToken` + `calendarUrl` in the JSON response; `InviteClaim.tsx:148-161` renders both on screen as **React state, `null` on reload**. **So a trusted helper sees both links exactly once, on a screen they will close, and has no durable copy of either.** ⚠️ **Arguably worse than #013, because this cohort is phone-only by construction.** **Also confirmed: on the PUBLIC claim path `slots.ts:169` deliberately withholds `cancelToken` from the response and `SupportPage.tsx` has no release link — so the confirmation EMAIL is the only delivery vehicle for the release link that exists anywhere. No email means no release link, ever.** | 22 Aug 2026 (Claude Code, during the #013 investigation) | 🔴 High | **✅ CLOSED — merged and verifiable on `main`** (confirmed by the 22 Aug banked-vs-shipped audit).
| 028 | 🟢 | **A page opened before it's activated says it DOESN'T EXIST.** `pages.ts:30` returns 404 with *"This support page isn't available yet."* for any non-active page — but `SupportPage.tsx:97` **discards that message** and renders one generic branch for every 404: *"This page doesn't exist or has been removed. If someone shared a link with you, please double-check it."* **Who sees it: anyone opening a `/s/:slug` link before the recipient activates** — a helper sent the link early, or anyone holding a link to a page scheduled for a future go-live. ⚠️ **They're told the page doesn't exist, so they conclude it's a mistake or a scam and don't come back.** **Fix is logic, not copy — the frontend must distinguish 404 reasons.** ⚠️ **Plausible this week: if Kate shares her friend's page link before the friend activates it, this is what the friend's helpers would see.** | 22 Aug 2026 (found by Claude Code while applying the live-copy fixes) | 🔴 High | ⚡ **22 Aug: THE FIX IS BUILT AND SITTING IN AN OPEN PR (#64).** **One merge, no work.** ✅✅ **CLOSED — PR #64 MERGED, confirmed by Kate 23 Aug.** A page opened before activation now says it isn't live yet, instead of claiming it doesn't exist. **Found in the 22 Aug gift rehearsal, fixed the same night.**
| 027 | 🟢 | **Is migration `0010` applied to production? Nobody knows — and if it isn't, every claim has been failing silently since 12 August.** PR #55 merged `0010` on 12 Aug at **16:58**, *after* that day's audit baseline (`e065d93`, 12:35), so **`0010` has never been audited by anything**. `slots.calendar_token` is written **on every claim, both paths** (`slots.ts:117`, `invites.ts:269`) **with no try/catch** — so an unapplied `0010` means every claim throws `42703` from ~17:00 on 12 Aug, **invisibly**, because error monitoring is still unbuilt. `0009` shipped with a runbook PR; `0010` merged 56 minutes later with none. **One read-only query settles it and it precedes all other work.** | 22 Aug 2026 (Claude Code reconciliation, `RECONCILIATION_22AUG.md`) | 🚨 Critical | **✅ CLOSED 22 Aug 2026 — `0010` IS on production; claims were never broken.** Proved read-only without a working prod credential (local `.env` password dead since the 26 July rotation): `GET /api/calendar/<bogus>.ics` on the live API returned **404 with the calendar-specific message**, reachable only *after* the `WHERE slots.calendar_token = $1` query succeeds — and with no try/catch on the handler, a `42703` could only have returned a 500. `0010` contains exactly two objects (the column + a UNIQUE index) in one transaction. ⚠️ **Residuals:** the UNIQUE index is inferred not observed, and the migrations journal couldn't be read — three read-only queries in the Neon SQL editor close both (see TRACKER session log, 22 Aug early afternoon). 🚩 **The finding that outlives this bug: had it been broken, nobody would have known. Error monitoring is still unbuilt.**
| 026 | 🟡 | **Cron failures are BACK — `dispatch-invites` timing out, and the failure mode has changed.** The 6/7 Aug cron incident was closed as *fully resolved* (PR #40 — `ensureDbAwake()` + staggered schedules). On 20–21 Aug cron-job.org is again emailing repeated failures for `AL - dispatch-invites` (`/api/internal/dispatch-invites` on `workspaceapi-server-production-7a93.up.railway.app`) — but now the status is **`Timeout`**, not the fast `500`s of August: scheduled `00:00:00 GMT`, last attempt `00:00:33 GMT`, i.e. cut off at cron-job.org's ~30s ceiling. Failures recur near the top of each hour with successes in between, so it's intermittent. **🚩 Two live hypotheses, neither confirmed.** (1) **The stagger has come undone** — the email says scheduled at minute **00**, but the 7 Aug staggering put `dispatch-invites` at **2/17/32/47**; at minute 00 it collides with `dispatch-scheduled` (0/15/30/45), which is exactly the cold-Neon stampede the staggering existed to prevent. Kate can confirm or kill this in 2 minutes in cron-job.org, no code. (2) **PR #40's own fix is overshooting** — `ensureDbAwake()` waits 1s→2s→4s across retries on top of a 10s pool connect timeout, so a genuinely cold start may now spend longer than 30s retrying politely; that would have converted a fast, loud `500` into a slow request cron-job.org gives up on, which fits the changed signature precisely. **Not currently costing anything** — Kate confirmed 21 Aug there are still no users. **But this is the job that sends helper invites**, and paired with bug #009 (sends are fire-and-forget and stamp `sent` even when they fail) a silently failing invite dispatcher at launch is the "silent-never-sends landmine" TRACKER already named. Note: raising cron-job.org's per-job timeout is **not available** on the free plan (confirmed 7 Aug). Read-only investigation prompt banked at `build/CRON_TIMEOUT_RECURRENCE_PROMPT.md`. **🔎 UPDATE 21 Aug (evening) — cron-job.org dashboard read, and the evidence has moved.** Kate checked the dashboard: last 5 events all green `200 OK`. **But the dashboard is a 25-event rolling window (~1.5 hours at these frequencies), and the failures were this morning** — green *now* is not green *today*. **(a) Collision hypothesis: weakened.** Inferred from event minutes, the four jobs are still on distinct minutes — but **not the minutes TRACKER records from 7 Aug**: `dispatch-claim-notifications` (:36,:51) and `activate-scheduled-pages` (:49) match, while `dispatch-invites` now looks like **0/15/30/45** (not 2/17/32/47 — consistent with the failure email's `scheduled 00:00:00`) and `dispatch-scheduled` like **10/25/40/55** (not 0/15/30/45). No visible collision, but the recorded schedule is stale and needs reading directly, including any 5th/6th job. **(b) 🚩 NEW AND STRONGER — the green runs aren't healthy.** Event seconds imply durations of **~17s for `dispatch-invites` and `dispatch-scheduled`**, ~6–8s for the other two. August's warm-database baseline was **~1 second**. A 17-second run is already past halfway to cron-job.org's 30s cutoff — so the failures aren't random, the whole system has drifted slow and the two slowest jobs tip over. 17s also fits `ensureDbAwake()`'s own arithmetic uncomfortably well (a 10s pool connect timeout + 4s backoff + success ≈ 17s), i.e. **the fix may be running its full retry ladder on nearly every cycle** — working as designed, just not fast enough to be safe. *Inferred from timestamps, not yet confirmed — click DETAILS on a run to read the real duration.* **(c) 🚩 Why `dispatch-invites` specifically.** On the inferred schedule (invites 0/15/30/45, scheduled 10/25/40/55, activate 4/19/34/49, claim-notifs 6/21/36/51), the gap before **every** `dispatch-invites` run is **exactly 5 minutes** — precisely Neon's autosuspend timer. It is the one job that reliably arrives at a database that has just gone to sleep. Every other job has a 4-minute or shorter gap ahead of it. **Cheapest possible fix if confirmed: move `dispatch-invites` ~2 minutes earlier (e.g. 13/28/43/58) so no gap ever reaches 5 minutes.** Free, no code, no deploy. Trade-off to check: a database that never sleeps burns Neon compute hours. **🔑🔑 UPDATE 21 Aug (later) — KATE HAD RUN OUT OF NEON CREDITS, and has now upgraded to the Launch plan. This reframes everything.** Neon's Free plan is **100 CU-hours/project/month**, and when it's exhausted *"the project's compute is suspended until the next billing period or until you upgrade. Existing connections drop and new ones can't open."* That's **binary and total**, not gradual — so it explains any block of *consecutive* hard failures, but **not** the intermittent pattern with 17-second successes in between (those remain cold starts). Both were probably happening at once. **🔁 The uncomfortable full circle: the 7 Aug fix is the likely cause of the credit burn.** Staggering four jobs across the hour left a maximum quiet gap of 5 minutes — exactly Neon's scale-to-zero timer — so the compute went from mostly-asleep to **almost never asleep**. At the 0.25 CU minimum that's ~180 CU-hours/month against a 100 CU-hour allowance; from 7 Aug at ~6 CU-hours/day, 100 hours runs out around **20–24 Aug**. The failure emails start **20 Aug**. Arithmetic is an estimate — **Neon's billing/usage page shows the real numbers and the exact suspension date, which confirms or kills this in one look.** **✅ What the Launch plan changes:** scale-to-zero **can be disabled on Launch** (on/off only — custom durations are Scale-plan only; Free can't disable it at all). Disabling it makes the database always warm, which removes the cold-start root cause outright rather than dodging it. Cost at 0.25 CU running 24/7 ≈ 180 CU-hrs × US$0.106 ≈ **~US$19/month** — and since the current schedule already keeps it awake almost constantly, that's close to what's being spent anyway, without the reliability. **⚠️ Supersedes the earlier 'shift `dispatch-invites` to 13/28/43/58' suggestion** — that fix deliberately keeps the database awake 24/7, so it costs the same as disabling scale-to-zero while leaving the knife-edge in place. Strictly worse now she's on Launch. Kept on the record as a correction, not deleted. **⚠️ NEW RISK — Launch is usage-based with no minimum and no automatic ceiling.** Set **spending notifications** (email alerts on approaching a dollar amount). Do **NOT** set a hard *consumption limit* on the production project: hitting one *"automatically suspends all active computes… until the next billing period starts"* — i.e. it recreates the exact outage just escaped. **✅✅ CONFIRMED 21 Aug (night) — Kate checked Neon: the project WAS suspended TODAY (21 Aug), and is up and running again after the upgrade.** **This confirms the two-causes reading, and the dates split it cleanly:** the failure emails begin **Thu 20 Aug** (4:02pm, 6:03pm, 7:03pm, 11:03pm) — *before* today's suspension — so **Thursday's failures were cold-start timeouts**, while **some or all of Friday's were the suspended project**. Suspension also produces `Timeout` rather than a clean error, because a compute that won't accept connections leaves the handler waiting on `ensureDbAwake()`'s retry ladder until cron-job.org hangs up — which is why the two causes looked identical from the outside. **Residual proof the cold-start half is still live:** the 8:45pm dashboard run succeeded *after* the upgrade and still took ~17 seconds. Upgrading fixed the suspension; it did not fix the slowness. **Status now: 🟡 in progress.** DONE — upgraded to Launch (suspension cleared) · spending notification set at $20. **STILL TO DO — disable scale-to-zero on the production compute** (`ep-spring-term-a7b3ms45`), which is the actual fix for the cold starts. **Gotcha found:** Neon's *project* Settings → **Compute defaults** only applies to computes created in **future** and will NOT change the existing one — the setting Kate needs is per-compute: **Branch selector → production branch → Postgres database → Computes → Edit → scale to zero → save.** Only the production branch; leaving it enabled on sandbox/disposable branches is correct and saves money. **⚠️ Note on the $20 alert:** always-on 0.25 CU is ≈ US$19/month, so a $20 notification will fire almost immediately once scale-to-zero is off. Fine as a first-month learning tripwire; raise to ~1.5× the real baseline once known, so the alert means *abnormal* rather than *Tuesday*. **Not closable until it runs green for a full week** — and the endpoint-robustness work (respond-fast/background, double-send and never-send protection vs bug #009) is still outstanding regardless. **🚩🚩 NEW HYPOTHESIS 21 Aug (night), possibly bigger than cold starts — CROSS-PACIFIC LATENCY.** Neon's console shows the database in **AWS Asia Pacific 2 (Sydney)**. Railway's default deploy region is **US West**, and Railway has **no Australian region at all** (only `us-west2`, `us-east4`, `europe-west4`, `asia-southeast1`/Singapore). If `@workspace/api-server` is running in a US region, **every single query crosses the Pacific** — ~180–200ms round trip. A handler running ~90 sequential queries would spend **~17 seconds purely on travel**, doing no work. **The slow jobs took ~17s; the light ones took ~6s — and the two slow ones (`dispatch-invites`, `dispatch-scheduled`) are precisely the two that loop over rows *sending* things**, i.e. the most round trips. This would also mean the whole site is slow for every Australian visitor, not just the crons. **Check: Railway → `@workspace/api-server` → Settings → Deploy → Regions.** *Unconfirmed — needs the region read and the query count in the handler.* **Note the architectural point if it's confirmed:** co-locating app and database matters far more than being near users — a page load is one user round trip but dozens of database round trips. Closest Railway region to Sydney is **Singapore (~95ms)**; true co-location would mean moving the Neon project to Singapore too (new project + data migration — a real job, not a toggle). `api-server` has **no volume**, so a region change there is settings + redeploy; the Umami group's Postgres/Valkey **do** have volumes and would need migration with downtime — leave them alone, they're only analytics. **🚩 SEPARATE COST RISK found on the same screen: compute is set to autoscale `.25 ↔ 8 CU`.** At 8 CU that's ~US$0.85/hour ≈ **US$600/month** if anything ever pinned it there — a loaded gun on a usage-based plan with a pre-launch app and no users. **Recommend dropping the maximum to 1 CU (2 at most)**, same Computes → Edit screen as the scale-to-zero toggle; raise it in seconds if real traffic ever needs it. **🚩 ALSO NOTED: Neon history retention is 6 hours** — that's the point-in-time restore window. Thin for a live product holding customers' data, where a problem might not be noticed until next morning. Review before launch (not tonight). **✅🚩 CONFIRMED 21 Aug (late) — THE API SERVER WAS IN US WEST.** Kate checked Railway: `@workspace/api-server` was deploying to **US West**, with the database in **Neon Sydney**. **Every query was crossing the Pacific (~180ms round trip).** The latency hypothesis is now a confirmed *condition* — what remains unproven is how much of the 17 seconds it accounts for, which depends on the sequential query count per handler (Q0 of the prompt). **Kate has switched the region setting to Singapore but has NOT redeployed** — Railway applies a region change **on the next deployment**, so it is **not yet live**. Redeploy via *service → Deployments → latest → ⋯ → Redeploy* (brief restart; harmless pre-launch). **📐 TESTABLE PREDICTION, worth recording before the redeploy so it can't be rationalised afterwards:** Singapore→Sydney is ~95ms vs US West→Sydney ~180ms, i.e. **roughly half**. **If latency is the dominant cause, `dispatch-invites` should fall from ~17s to ~9s.** If it barely moves, latency is a side-show and the cold start / query count is the real driver. Either result is informative — **but only if one thing changes at a time.** **⚠️ Neon changes made in parallel (max CU 8→1, plus one toggle switched off) partially muddy that experiment** — noted honestly rather than pretended away. **❓ OPEN — WHICH NEON TOGGLE?** Kate reports turning off "the auto thing". The *Computes → Edit* screen carries **two** switches and they do opposite jobs: **Autoscaling** (lets the compute grow under load) and **Scale to zero** (lets it fall asleep when idle). **Only turning off *scale to zero* fixes the cold starts.** Turning off *autoscaling* alone leaves the 5-minute sleep cycle fully intact. **✅ ANSWERED — Kate confirms it was SCALE TO ZERO.** The right switch. **The database is now always warm and the cold-start cause is removed.** Neon state: max CU **1**, scale to zero **off**. Expect the $20 spending notification to fire shortly (always-on at 0.25 CU ≈ US$19/mo) — that's the alert working, not a fault. **🔬 THE EXPERIMENT IS ACTUALLY CLEAN — because the redeploy hasn't happened.** Right now exactly ONE variable has changed: scale-to-zero is off, and the API is **still in US West**. So the next few cron runs isolate the cold-start contribution perfectly, for free, with no further action. **Read the durations before redeploying.** **Step 1 (now, US West + always warm):** ~1–2s → cold starts were the whole story, latency barely matters · ~9s → both contribute · **still ~17s → latency dominates and the August `ensureDbAwake()` work was treating a symptom all along.** **Step 2 (after the Singapore redeploy):** whatever step 1 gives should roughly **halve** if latency is proportional (180ms → 95ms). Recording both predictions in advance so neither can be rationalised after the fact. | 21 Aug 2026 (Kate's cron-job.org failure emails, screenshot) | 🟡 In progress — suspension cleared, **cold-start fix still to apply**; launch blocker until green for a full week |
| 025 | 🔴 | **PR #58 claim notifications don't fan out — and fire to NOBODY on crisis/organiser pages.** Found during PR #58's disposable-branch verification, not in production yet (PR unmerged). Section D widened `notifyRecipientOfTaskEvent` (release/reschedule/note events) to reach every active grant-holder — but "someone claimed a task," the single most common notification, goes through a separate, older, batched function (`sendRecipientClaimNotification`, `internal.ts:511`) that section D never touched. It only reads `page.recipientEmail` and is skipped entirely when that's null — which crisis/organiser pages always are unless section E's "yes, ready" path was used. **Consequence: on a friend-run crisis/bereavement page — the exact case Item 18 was built to fix — claiming a task notifies nobody at all**, not the recipient, not the manager. Traces back to a factual error in Part 1's original investigation (assumed `notifyRecipientOfTaskEvent` was THE single fan-out point for every event, including claims — it isn't). **This is a "do not merge PR #58 until fixed" item, not a follow-up.** Paste-ready fix + re-verify prompt banked at `build/PR58_FIX_CLAIM_FANOUT_PROMPT.md`. | 12 Aug 2026 (PR #58 disposable-branch verification) | 🔺 Critical while PR #58 is open — blocks merge, not yet live since unmerged |
| 024 | 🟢 | **Checkout copy promises "nothing else, ever" to the recipient — not actually true.** On the gift purchase form ("Who's this for?"), the microcopy under "How should we reach them?" reads: *"We'll send their gift here — nothing else, ever."* But the recipient's own contact IS used for more than the one-time gift delivery — every task-event notification (claim, cancel, reschedule via `notifyRecipientOfTaskEvent`), the calendar link, and (once Item 18 lands) manager notifications all go to that same email/mobile. A buyer reading "nothing else, ever" reasonably expects total silence after the one gift email — that's not how the product works. Flagged by Kate reviewing the actual checkout screen. **This is a live, factual promise on the money path, made to every buyer today** — worth treating as more than cosmetic. Fix direction: rewrite to something accurate that keeps the real intent (no spam, no selling their details, no marketing) without promising total silence — e.g. *"We'll use this to send their gift and keep them posted on their page — never anything else."* Exact wording is Kate's call. Low-risk fix (one static string on the checkout screen, no payment/Stripe logic touched), but flagged clearly since checkout is otherwise guardrailed off in every build brief this session. | 12 Aug 2026 (Kate reviewing the checkout screenshot) | 🔺 High — live and factually wrong on the actual purchase screen; cheap to fix | ⚡ **22 Aug: THE FIX IS BUILT AND SITTING IN AN OPEN PR (#65).** The prompt DID run — it produced `claude/bug-024-reorder-buy-form`. **One merge, no work.** ✅✅ **CLOSED — PR #65 MERGED to `main` as `67984b3`, 23 Aug 09:56 UTC.** Live copy is now: *"We'll use this to send their gift, remind them it's waiting, and let them know when someone steps in."* **The promise clause was dropped entirely rather than reworded — reasoning banked in `COPY_BANK.md.md` so nobody re-adds it.** Shipped alongside the buy-form reorder (recipient block first) and an `autoFocus` correction.
| 002 | 🔴 | **Gift-for-yourself confusion:** Kate bought Aunt Lucy for herself, then received an email saying "someone has bought this for you" — the gifting copy/logic doesn't correctly handle the self-purchase path. ⚠️ **6 Aug 2026 — Kate flagged this may not be a real bug:** she'd used the **same email address for both the buyer and the recipient** in that test walkthrough, which could fully explain it as expected behaviour for that path, not broken self-purchase logic. **12 Aug 2026 — reconfirmed, with screenshots, on a deliberate self-purchase test (testing PR #54).** Kate bought for herself again and got both standard templates, neither aware buyer = recipient: the buyer-confirmation email ("You've just given KT people who show up," greeted "Hi KT") and the gift-delivery email ("Someone's got you," also greeted "Hi KT") — reading oddly back-to-back when there's no separate "someone." **This still isn't the clean two-different-emails re-test Kate flagged as needed 6 Aug** — that's a distinct, still-open question (does the ORIGINAL accidental-confusion report happen when buyer and recipient are genuinely different people). What this DOES settle: when buyer and recipient are the same person — accidental or, as here, deliberate — neither template accounts for it at all. **Open product question for Kate, not decided here:** is self-purchase a use case worth writing for (e.g. a self-aware copy variant), or worth a soft checkout nudge ("looks like you're buying this for yourself — is that right?"), or just an edge case to leave as-is since the product's built for gifting? | 1 Aug 2026 (Kate's real-purchase walkthrough); reconfirmed 12 Aug 2026 (self-purchase test during PR #54 review) | 🔴 High — reconfirmed present; **still pending the separate two-different-emails test for the original accidental-purchase report** |
| 003 | 🔴 | **Receipt has no next steps:** the purchase receipt doesn't tell the buyer/recipient they need to log in, or where to find the activation link — that info only exists in a separate email, easy to miss or lose | 1 Aug 2026 (Kate's real-purchase walkthrough) | 🔴 High | ⚠️ **CONFIRMED STILL LIVE 22 Aug 2026: neither the Stripe confirmation screen nor the receipt tells the buyer what happens next. The Aunt Lucy email does explain it — the Stripe screen is a dead end.**
| 004 | 🔴 | **Trusted helpers — asked for but not buildable:** the setup flow asks whether a task needs "trusted helpers," but there's no facility anywhere to actually create or manage a trusted helpers list. The question has no answer path. ⚠️ **9 Aug 2026 — recon found the facility DOES exist in code** (per-task "Trusted people only" toggle + `ALWAYS_TRUSTED` lock + trusted SMS invites + claim-by-token, all merged). Kate to re-walk the setup→trusted flow, then close or re-scope. **12 Aug 2026 — 12 Aug reconciliation re-checked: nothing's moved, code unchanged since 9 Aug.** Still exactly the same product call, still Kate's to make on the re-walk — not stale from code drift, just still waiting on that walkthrough. | 1 Aug 2026 (Kate's real-purchase walkthrough) | 🔺 Critical — **likely stale, pending Kate's re-walk** |
| 009 | 🔴 | **Email sends are fire-and-forget — failures silently marked "sent":** during the 9 Aug outage, Neil's `gift_delivery` row in `gift_messages` was stamped `status='sent', sent_at=2:56pm` even though the email never arrived (Resend domain was unverified — every send was failing). No retry, no failure status, no alert; and the buyer receipt has NO queue row at all (pure fire-and-forget), so it died leaving nothing to retry. Consequence: any future Resend hiccup silently eats emails while the DB claims success. Fix direction: check/record the send API response (mark `failed`, not `sent`), retry failed rows via the existing cron, and queue the receipt like other messages. Pairs with the error-monitoring item (matrix P2 #15). | 9 Aug 2026 (bug #007 post-mortem via `gift_messages`) | 🔴 High — silent-failure class; schedule right after launch gates |
| 012 | 🟢 | **Helper told "baby has arrived" before the birth — PR #54 OPEN, built, not yet merged or reviewed.** Full history: found in testing 9 Aug → confirmed via code investigation 10 Aug → fix direction decided 10 Aug → built same day. **What shipped in the PR (`claude/pre-birth-invite-copy`):** migration `0009_pre_birth_invite_copy.sql` (additive/nullable — `support_pages.trusted_line` text + page-local `baby_stage` enum, NOT applied to production yet) · the "has the baby arrived yet?" toggle + both invite-line fields as real placeholders (not pre-filled values) on the activation screen · **a bonus fix found along the way: `situationLine` had a working PATCH endpoint but was NEVER actually surfaced in the `/manage` UI** — Claude Code built a whole new "How Aunt Lucy writes to your people" section there, which is also what makes the `baby_stage` flip-after-the-birth promise real (blank fields now store as NULL, not a baked default, so the send-time fallback stays stage-aware after a later edit) · illness_recovery's gift-delivery line reviewed and left alone (agreed already safe pre-surgery) · typecheck + rally/api-server builds clean · no bereavement/ongoing_support/other copy or payment code touched. **Not yet checked: any visual click-through** — the activation/manage screens can only be exercised against a real paid gift on the production DB, which was deliberately not touched, so this needs a proper look at the PR's Vercel preview before merging, not just a code read. All new wording flagged 🆕 in the PR for Kate to bless — the gift-delivery `new_baby` line most of all, since Claude Code changed it from the suggested third-person phrasing to second-person to match the surrounding paragraph. | 9 Aug 2026 (baby-flow test day); confirmed 10 Aug; fix decided 10 Aug; built 10 Aug — PR #54 | 🔺 Critical while open — built, awaiting Kate's review + merge, then a separate backup-first migration runbook | **✅ CLOSED — PR #54 MERGED and migration `0009` applied; confirmed by the 22 Aug banked-vs-shipped audit. The row had been left reading "PR #54 OPEN" for ten days.**
| 013 | 🔴 | **Helper has no way to change their claimed time or cancel it — only the buyer/organiser can.** On the claimed-slot screen, a helper (not the person who bought the page) has no visible option to reschedule or cancel their own claim. Note: a helper "can't make it" release route was built and shipped 6 Aug (PR #38) — this finding may mean that route isn't surfaced on this particular screen (email-only link?), doesn't cover changing the time (cancel-only), or has regressed. Needs investigation before scoping a fix, not just a rebuild. **12 Aug 2026 — 12 Aug reconciliation narrows this down:** both un-claim (`#38`, `cancel_token`) and reschedule (`#49`, `slot_flexibility`) genuinely exist in code, unchanged. Reads as most likely a *surfacing* gap — neither route is shown on the helper's own claimed-slot screen — not missing functionality. Investigation should start there before any rebuild. | 9 Aug 2026 (baby-flow test day) | 🟡 Medium-High — helper self-service gap, most likely a surfacing gap over existing #38/#49 functionality, per 12 Aug reconciliation | ⚠️🔺 **STRENGTHENED 22 Aug 2026 (Claude Code reconciliation): it's worse than a missing button — phone-only helpers get NO claim confirmation at all.** `email.ts:425` silently returns when the contact isn't an email address, so **the release link never reaches them**. That makes both un-claim (PR #38) **and** the calendar feed (PR #55) **unreachable for SMS helpers — on the SMS-first crisis path.** By Kate's own criterion this now **outranks the helper-reminders work.**
| 014 | 🔴 | **Claimed spots should sort to the top of the recipient's page, not the bottom.** When the recipient clicks back into their page, currently-claimed slots render at the bottom of the list. Kate's ask: show claimed spots at the top so it's easy to see what's covered at a glance. | 9 Aug 2026 (baby-flow test day) | 🟡 Medium — UX polish, quick win | ⚠️ **CONFIRMED STILL LIVE 22 Aug 2026 in the gift rehearsal — claimed tasks still render at the BOTTOM of the public page. Asked for on 9 Aug.**
| 015 | 🟡 | **Old green branding, not the new terracotta — now confirmed in a THIRD template.** The email used for setting up the page still uses the old green colour scheme, not the terracotta rebrand shipped in PR #52 (9 Aug first-impressions pass). Fix direction: sweep all outbound email templates for terracotta/brand consistency against #52's scope. **12 Aug 2026 — two more confirmed with screenshots.** The "Someone's got you" gift-delivery email: header terracotta, but the "Take a look" CTA button still old dark green. **12 Aug 2026, later — a third:** the helper claim-confirmation email ("Thanks for helping [name] — here's what you've signed up for") is old green **throughout** — header band AND the "release this slot" link both still the old green, no terracotta anywhere on this one. **This is now 3 templates deep and growing — worth treating as a full sweep of every outbound template against #52's brand scope, not one-off patches per template as they're found.** Worth doing alongside #012's fix where the file overlaps (gift-delivery email), but the claim-confirmation email is a separate template/file and needs its own pass regardless. | 9 Aug 2026 (baby-flow test day); 2 more templates confirmed with screenshots 12 Aug 2026 | 🟡 Medium, trending up — 3 templates confirmed, a full sweep is now the right scope, not spot fixes |
| 018 | 🔴 | **"Something else" custom task tile has its fields in a confusing order.** On the add-a-task screen, choosing "Something else" from the task-type picker should let someone type what they actually need — but the free-text "What would actually help?" field renders ABOVE the type picker, not under/attached to the "Something else" option. Kate's read: it currently looks like you're meant to type first, then pick a type second, which doesn't make sense. Fix direction: either make "Something else" the field's own heading, or move the free-text box to appear directly under the "Something else" option once it's selected, so the order reads as pick → then specify. | 12 Aug 2026 (PR #54 activation testing) | 🟡 Medium — setup-flow confusion, likely a quick reorder |
| 019 | 🔴 | **Pronoun picker order — "she/her" should be the top option, not "they/them."** On the "When Aunt Lucy writes to your people" section, the "Refer to you as" dropdown's current order isn't what Kate expects; she wants she/her listed first, they/them last. | 12 Aug 2026 (PR #54 activation testing) | 🟢 Low — quick dropdown-order fix | ⚠️ **CONFIRMED STILL LIVE 22 Aug 2026 in the gift rehearsal — the dropdown still defaults to they/them. Asked for on 12 Aug, never fixed.**
| 020 | 🔴 | **Copy suggestion — "Ready when you are" screen.** Kate's proposed rewrite: "You've selected 6 ways people can help (you can change this at any time)" in place of the current "6 ways people can help. You can change all of this later." Flagged as a copy suggestion, not decided. | 12 Aug 2026 (PR #54 activation testing) | 🟢 Low — copy tweak only |
| 021 | 🔴 | **"Add someone" (inviting people) is buried at the bottom of a long scrolly page.** Clicking through to add people lands on a page titled "Your people" that stacks "Support is on the way," the full "Your tasks" list (all 6, with edit/delete), and "Where should we send updates" all ABOVE the actual "Add someone" form — the one thing Kate clicked through to reach. Kate's read: this should either be its own page, or the add-people form should be at the top where it's expected. Real usability issue, not polish — inviting people is core to what the product does. | 12 Aug 2026 (PR #54 activation testing) | 🟡 Medium-High — core-flow IA problem, buries the primary action on its own page |

---

## Fixed bugs

| # | Status | Description | Found | Fixed |
|---|--------|-------------|-------|-------|
| 023 | 🟢 | **Migration `0010` merged-but-unapplied — same failure shape as #022.** PR #55 (subscribable calendar link) merged to `main`. `slots.ts` sets `calendar_token` unconditionally on every claim and `calendar.ts` reads it, but that column only existed on the disposable Neon sandbox — never applied to production before the merge. Because Railway auto-deploys on every push to `main`, the public claim path was at risk of throwing `42703 undefined_column` for real helpers. Flagged proactively by the Claude Code session itself, immediately after merging, before a customer complaint surfaced it — genuinely good practice. **Second occurrence of this exact pattern in one day** (see #022) — worth a standing habit going forward: apply the migration at or immediately before merge, not after. | 12 Aug 2026, night (self-flagged by Claude Code right after the PR #55 merge) | **12 Aug 2026, night — Kate applied migration `0010` herself via the Neon SQL Editor** (backup-first: safety branch `backup-before-0010`, SQL vetted as additive-only before running, applied, both verification queries confirmed 1 row each — `calendar_token` column and `slots_calendar_token_unique` index both present). No credential ever left the browser — applied directly in Neon's editor rather than handing a connection string to Claude Code, so no rotation needed this time. |
| 005 | 🟢 | **School pickup task has no time field** — turned out the generic `slot_time` field already existed everywhere except the gift-activation review screen, which had no time input at all. No schema change needed — reused `slot_time`. | 1 Aug 2026 (Kate's real-purchase walkthrough) | **6 Aug 2026** — PR #37 (`claude/bugs-5-6-task-details`) built, migration `0005` applied to production + verified, PR **merged and live**. |
| 006 | 🟢 | **Meals task doesn't prompt for dietary requirements or headcount** — genuinely missing everywhere; only free-text `notes` existed. New nullable `dietary_notes` + `headcount` columns. | 1 Aug 2026 (Kate's real-purchase walkthrough) | **6 Aug 2026** — same PR #37. Migration `0005_bugs_5_6_task_details.sql` applied to production (`ep-spring-term`), backed up first via Neon safety branch `ep-ancient-union`. PR **merged and live**. |
| 007 | 🟢 | **MONEY PATH — payment succeeded, nothing delivered** (first real $59 purchase, 9 Aug): charged OK, webhook OK, gift row written OK — but every email died. Root cause: `send.auntlucy.com.au` had lost its MX record → Resend sending domain unverified → all sends failing since ~2:55 pm (the "email outage" was this). Masked by bug #009 (failures stamped `sent`). | 9 Aug 2026 | **9 Aug 2026, same evening** — missing MX re-added at VentraIP, SPF confirmed intact, domain re-verified in Resend; Neil's `gift_delivery` row flipped `sent`→`scheduled` via one-row UPDATE and the cron delivered it. **Confirmed arrived in Neil's inbox.** Buyer receipt unrecoverable (no queue row — see #009). Fixed with zero code changes. |
| 008 | 🟢 | **Checkout showed "Tax A$0.00" despite GST-inclusive pricing.** Root cause: automatic tax was on and the price tax-inclusive, but no Australian GST registration existed in Stripe Tax — the calculator had nowhere it was registered, so it computed $0. | 9 Aug 2026 (first real $59 purchase) | **9 Aug 2026, same night** — Australia (GST) registration added in Stripe → Tax → Registrations; live checkout now shows "GST A$7.18" on the $79 link (exactly 1/11 — correct inclusive maths), total unchanged. Icebreaker's 3 tax-off subscriptions deliberately untouched (accountant-sitting question). No code changes. |
| 010 | 🟢 | **Gift delivery email ignored the occasion** — illness gift arrived with new-baby copy (default was baby-only). | 9 Aug 2026 (Neil's recovered gift email) | **9 Aug 2026, same night — PR #52 merged.** One combined occasion-aware paragraph per occasion (bereavement / baby / illness-recovery-surgery / neutral fallback AS DEFAULT), giver credited by name, Kate approved every render (3 revision rounds). Copy of record in COPY_BANK v3. |
| 001 | 🟢 | ~~Replit internal hostname not accessible externally (preview broken)~~ | Earlier 2025 | **12 Aug 2026 — closed, not applicable.** Kate confirmed Aunt Lucy has fully moved off Replit onto Vercel (frontend) + Railway (backend); there is no Replit preview left to be broken. Kept here per house rule (archive, don't delete) rather than removed outright. |
| 022 | 🟢 | **"Make it live" activation failing** — root cause: PR #54's migration `0009_pre_birth_invite_copy.sql` (adds `support_pages.trusted_line` + `baby_stage`) shipped code that writes those columns on every activation, but the migration itself was deliberately left unapplied to production, so every activation hit `42703 undefined_column` → generic 500 → "That didn't go through." Confirmed line-by-line by Claude Code from the deployed code (`gifts.ts:564-565` → uncaught → global error handler); the phone/email-reuse theory was formally ruled out (no uniqueness constraint anywhere in the activation path). | 12 Aug 2026 (post-PR #54 merge activation test) | **12 Aug 2026 — Kate applied migration `0009` herself via the Neon SQL editor** (backup-first: safety branch `pre-0009-safety`, pre-check confirmed 0 rows/missing columns, applied, verified, re-tested). Activation confirmed working again. No prod DB credentials were ever given to Claude Code — matches house discipline. |
| 011 | 🟢 | ~~**Promo code not prominent enough — tester paid real money by accident.** During the baby-flow test day roleplay, Kate meant to give the tester a free promo code, but the promo/discount code field on checkout wasn't visible or obvious enough. The tester didn't spot it, went ahead, and paid full price — a real, unintended charge. Fix direction: make the promo-code entry point clearly visible on the checkout/landing step (not buried), and Kate needs to refund the accidental charge separately (not a code fix).~~ | 9 Aug 2026 (baby-flow test day) | **12 Aug 2026 — closed, not a problem (Kate's call). Refund confirmed done (Kate, same night)** — the accidental 9 Aug charge has been refunded, nothing outstanding. Kept here per house rule (archive, don't delete) rather than removed outright. |
| 017 | 🟢 | ~~**Activation says "6 ways to help," live public page shows only 4.** On the "Ready when you are" confirmation screen, Kate had 6 tasks set up (meal, grocery run, hold the baby, load of washing, school pickup, short visit) and the screen said "6 ways people can help." Opening the actual public support-page link showed only 4 cards (meal, grocery run, load of washing, short visit) — "Hold the baby so I can shower" and "School pickup for the big kids" were both missing. Likely explanation, not yet confirmed: both missing tasks are kid-related and school pickup was explicitly locked to "Trusted people only" (the existing `ALWAYS_TRUSTED` rule from bug #004's facility) — if trusted-only tasks are deliberately hidden from the public "anyone can help" link and only shown via the separate trusted-helper invite, this is working as designed, but the "6 ways" copy on the activation screen is misleading since it doesn't say some of those 6 are trusted-only.~~ | 12 Aug 2026 (PR #54 activation testing) | **12 Aug 2026 — closed, working as designed (Kate confirmed).** The two missing cards were both locked "Trusted people only" tasks, correctly hidden from the public anyone-can-help link — exactly the likely explanation flagged when this was logged. **Left open, Kate's call, not auto-fixed:** the "6 ways people can help" copy on the activation screen still doesn't distinguish public-open from trusted-only tasks — if that reads as misleading later, it's a quick copy tweak (e.g. "4 open to anyone, 2 more for your trusted people"), not a rebuild. |
| 016 | 🟢 | **Recipient shown as "KT" on a purchase Kate made as "Kate."** Root cause: NOT a backend/data mixup — the server faithfully stored exactly what the form submitted. On a "for myself" purchase, a single "Your name" field feeds BOTH `purchaser_name` and `recipient_name` (`gifts.ts:180`), and that field held "KT" — almost certainly the **browser's own autofill** re-populating it from an earlier test, not a fresh value Kate typed. The originally-worried commercial risk (a repeat buyer, e.g. a doula, shown a *previous client's* backend data) does not exist — this was always a browser-autofill/no-second-look problem, never a data-isolation one. | 12 Aug 2026 (PR #54 activation testing) | **12 Aug 2026, evening — PR #56 merged.** `autoComplete="off"` added to every recipient field (so a remembered buyer value can't silently land there); `autoComplete="name"`/`"email"` kept on the buyer's own fields; a warm read-back confirmation added on the for-self path before checkout ("We'll set up Aunt Lucy for {name}…"), forcing a second look at the name, with any edit dropping the confirmation so a changed name always gets fresh eyes. Verified in a real browser against the real form before merge (a bogus `DATABASE_URL` served only the constant-backed tiers list; the actual purchase-confirming click was deliberately never pressed, so nothing touched a real database during testing). Frontend-only — Stripe, pricing, fulfilment, schema, sensitivity and trusted-contact logic all untouched. |

---

## How to log a new bug (give this to Cowork or Claude Code)

> "Log a new bug in BUGS_AND_FIXES.md: [describe what happened, what you expected to happen, and where it broke]"
