{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node ./scripts/preinstall.mjs",
    "dev": "dotenv -e .env -- concurrently -k -n api,web -c blue,green \"pnpm --filter @workspace/api-server run dev:local\" \"pnpm --filter @workspace/rally run dev:local\"",
    "db:push": "dotenv -e .env -- pnpm --filter @workspace/db run push",
    "db:seed": "dotenv -e .env -- pnpm --filter @workspace/scripts run seed",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.1.0",
    "dotenv-cli": "^8.0.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
